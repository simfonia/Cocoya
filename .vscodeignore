# Source Control & Configuration
.git/
.vscode/
.vscode-test/
.gitignore
.gitattributes
.DS_Store

# Dev dependencies
node_modules/
ui/node_modules/

# Standalone App Source (Tauri)
src-tauri/
!src-tauri/icons/128x128.png # VSIX icon

# Project Management
backup/
log/
docs/
FILE_STRUCTURE.md
GEMINI.md
*.code-workspace

# Build / Config files
tsconfig.json
package-lock.json
ui/package-lock.json
ui/package.json
ui/vite.config.js
ui/.gitignore

# Temp artifacts
temp_scripts/
out/test/
cocoya-*.vsix

# Note: We KEEP ui/src/ because VSIX currently references ESM scripts directly.
# Note: We KEEP resources/ because it contains deploy_mcu.py needed by VSIX.
# Note: We KEEP out/ because it contains compiled extension.js.
