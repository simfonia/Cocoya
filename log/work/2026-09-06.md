# 2026-09-06 工作日誌

## 任務：語系切換對齊主題切換——保留編輯區積木

### 需求
目前主題切換可以即時更換而不影響已存在編輯區的積木（透過 `snapshotWorkspaceForReload` + `reloadWebview` 機制），但語系切換會全部清除。需對齊兩者行為。

### 討論決策
- **根因**：`theme_manager.js setMode()` 有先呼叫 `snapshotWorkspaceForReload()` 才 `reloadWebview`，但語系切換的兩個 handler（工具列 `ui/src/ui/base.js` 與首頁 `ui/src/app/persistence.js`）直接 `reloadWebview` 未快照 → workspace 記憶體內容在 reload 時遺失。
- **方案**：在兩個語系切換 click handler 中，於 `reloadWebview` 之前加入 `snapshotWorkspaceForReload()` 呼叫（sessionStorage 快照，lifecycle `_restoreReloadSnapshot` 已於啟動時自動還原）。

### 異動檔案
| 檔案 | 內容 |
|------|------|
| `ui/src/ui/base.js` | 工具列語系膠囊 click handler 第 399 行後插入 `snapshotWorkspaceForReload()` 呼叫 |
| `ui/src/app/persistence.js` | 首頁語系膠囊 click handler 第 386 行後插入 `snapshotWorkspaceForReload()` 呼叫 |

### 技術深挖 (Technical Deep Dive)
- **快照機制**：`persistence.js` 的 `snapshotWorkspaceForReload()` 將 workspace 轉為 XML 並以 `sessionStorage.setItem(SNAPSHOT_KEY, ...)` 儲存（含 xml/filename/platform/isReadOnly）。sessionStorage 在 Same Origin 的 reload/重建後仍保留，生命週期為單次 tab。
- **還原機制**：`lifecycle.js _restoreReloadSnapshot()` 於 workspace 建立後呼叫 `consumeReloadSnapshot()`，有快照則載入 xml + 還原檔名 + 維持 dirty。
- **與主題切換完全對齊**：兩者現在走同一條路徑（snapshot → reloadWebview → restore），行為一致。

### 驗證
- `node --check ui/src/ui/base.js` ✓
- `node --check ui/src/app/persistence.js` ✓
- `npx vite build` ✓ built in 175ms
- [ ] 實機待測：VSIX + Tauri 下切換語系後編輯區積木保留、dirty 狀態保留

### 下次啟動方向 (Next Steps)
- 實機驗證語系切換（VSIX + Tauri）：開一個有積木的專案 → 切換語系 → 確認積木保留、dirty 狀態保留（可從 sessionStorage 手動檢查快照是否存在）。
`r`n`r`n## 追記：新增 Try/Except 例外處理積木（同日）`r`n- 需求：為基礎模組新增例外處理積木，PC 與 MicroPython 模式通用`r`n- MicroPython 文件確認：支援 try/except/else/finally 語法，可指定例外型別`r`n- 積木設計：py_try_except 包含 try body、except body、例外型別下拉選單`r`n- 放置分類：Logic（沿用 COLOUR_LOGIC 顏色）`r`n- 異動檔案：logic_blocks.js、logic_generators.js、toolbox.xml、i18n、docs/help/`r`n- 驗證：node --check + vite build 全綠`r`n- 實機待測：VSIX + Tauri 下拉選單、代碼產生、MicroPython 燒錄執行
`r`n`r`n## 追記：修正 Help 檔案與 BACKEND 下拉選單錯誤（同日）`r`n- [x] 更新 try/except Help 檔案：配色改為 Logic 分類色 (#b198de)，加入 param-table 表格樣式，對齊 py_ai_train_run 風格`r`n- [x] 修正 py_ai_train_run BACKEND 錯誤：加入 auto→local 遷移驗證（舊版工作檔可能有 auto 值但下拉選單已移除該選項）`r`n- [x] 驗證：node --check + vite build 全綠
`r`n`r`n## 追記：修正 BACKEND auto 雙防線 + Help 檔換行（同日）`r`n- [x] toolbox.xml 第 10 行 BACKEND 預設值 auto→local（工具箱拖出即報錯的根因）`r`n- [x] ai_inference_blocks.js：移除無效的 init() auto 驗證（init 執行時欄位值尚未從 XML 載入，永遠檢查不到），改以 FieldDropdown validator 攔截 auto→local（正確攔截舊工作檔殘留值）`r`n- [x] 重寫 py_try_except_zh-hant.html / en.html（先前 PowerShell 寫入造成 `r`n 字面字元、版面全亂，改用 editor 工具以真實換行重寫）`r`n- [x] 驗證：node --check + vite build 全綠
## 追記：examples dirty 狀態下「開新檔」流程中止修復（2026-09-06）

### 問題
Tauri 版實測：開啟 examples 下的 MCU xml → 修改成 dirty → 在 toolbar 按「開 PC 模式新檔」→ 出現存檔提醒按下「儲存」→ 跳出覆蓋 examples 警告按下「儲存」→ dirty 被清除，但編輯畫面停在原檔，沒有開出新檔。

### 根因（Technical Deep Dive）
`ui/src/bridge/tauri.js` 的 `case 'saveFile'/'saveFileAs'`（L188）在存檔路徑是內建範例目錄時，`save_file` 會拋出 `EXAMPLES_PATH` 錯誤，進入 catch 分支：

- 舊程式忽略 `_handleExamplesSaveDialog(xml)` 的回傳值，最後無條件 `return false`。
- 但 `_handleExamplesSaveDialog` 內部「覆蓋範例」成功時會 `dispatch saveCompleted`（→ `persistence.onSaveCompleted` → `setDirty(false)`，此為 dirty 被清除之來源）並回傳 `true`。
- 前端 `persistence.js _confirmSaveBeforeNew()`（L486）以 `saved !== false` 判斷存檔是否完成。因 `send('saveFile')` 回傳 false，此判斷結果亦為 false → 被視為「使用者取消」→ `startNewProjectFromHome()` 直接 early return，**開新檔流程被中止**。

→ 造成「存檔已成功（dirty 清除）但開新檔未發生」的矛盾行為。

### 修正
`case 'saveFile'/'saveFileAs'` 的 `EXAMPLES_PATH` 分支改為回傳 `_handleExamplesSaveDialog(xml)` 的真實結果（覆蓋成功 / 另存成功 → true；取消 → false），並於成功時補上 `_refreshAnchor()` 同步前端錨定。修正後 `_confirmSaveBeforeNew` 能正確判定「存檔已完成」，`startNewProjectFromHome` 得以繼續執行 `saveFileAs(newProject)` 開出目標平台新檔。

### 異動檔案
- `ui/src/bridge/tauri.js`（case 'saveFile'/'saveFileAs' 的 EXAMPLES_PATH 分支回傳真實結果 + _refreshAnchor）

### 驗證
- `node --check ui/src/bridge/tauri.js` ✓
- `npx vite build` ✓ built in 337ms
- [ ] 實機待測：Tauri 下「examples dirty 開新 PC 檔」應能存檔後自動開出新檔

### 下次啟動方向 (Next Steps)
- 實機驗證上述場景 2 端（覆蓋範例 / 另存新檔 兩條路徑）皆能在存檔後正常開出新檔。
## 追記：VSIX 同 family bug 檢查與修復（2026-09-06）

### 檢查結果
針對 Tauri「EXAMPLES_PATH 分支忽略回傳值 → 決策失準」的 bug，全面盤點相近流程：

**Tauri 端（3 個呼叫點，全部正確）：**
- `case 'saveFile'/'saveFileAs'`（L188）：✅ 本輪已修——EXAMPLES_PATH 分支回傳 `_handleExamplesSaveDialog` 真實結果。
- `_handleCloseDialog`（L1017）：✅ 原本即有 `const done = await ...; if (done)` 正確收 `done`。
- `_confirmSaveBeforeOpen`（L1153）：✅ 經 `send('saveFile')` 走已修的 case。

**VSIX 端（發現 1 個同 family 未修）：**
- `src/handlers/fileOps.ts` `performSave` 的 examples「另存新檔」分支：
  - 舊：`await this.handleSaveFileAs({ xml }); return true;` —— 即使 `handleSaveFileAs` 內另存對話框被取消（`!uri` 未寫檔）仍無條件回 `true`（**假成功**）。
  - 影響（與 Tauri 同 family 的「decision 失準」，方向相反）：`checkDirtyAndConfirm`（L21）`choice==='save' → return await performSave(xml)`；假成功回 true → `handleNewFile`（L72）/`handleOpenFile`（L82）/`handleCloseEditor`（L149）誤以為已存檔而繼續 `resetWorkspace`/開檔/關閉 → **取消另存後未儲存的變更遭丟棄**。
  - 註：VSIX `bridge.vsix.js send()` 是 fire-and-forget（postMessage 無回傳值），前端拿不到後端取消結果；但後端若回 false，至少 `handleNewFile` 等後端 handler 不會繼續破壞工作區。

### 修正（fileOps.ts）
- `handleSaveFileAs` 改為回傳 `Promise<boolean>`：真正寫檔成功回 `true`、取消回 `false`。
- `performSave` examples「另存新檔」分支改為 `return await this.handleSaveFileAs({ xml });`（去除無條件 `return true`）。
- `cocoyaManager.ts` `case 'saveFileAs'`（L181）呼叫忽略回傳，無副作用；其它呼叫方不受影響。

### 驗證
- `npx tsc --noEmit -p tsconfig.json` ✓（無錯誤）
- [ ] 實機待測（VSIX）：examples dirty → 開新檔 → 存檔 → 覆蓋範例警示選「另存新檔」→ 於另存對話框取消 → 應維持原工作區（不 resetWorkspace / 不開新）

### 下次啟動方向 (Next Steps)
- 實機驗證 Tauri 2 端（覆蓋範例 / 另存新檔）與 VSIX examples「另存取消」皆符合預期。
## 追記：開新檔「另存誤解」風險改善（方案 2，2026-09-06）

### 需求（使用者提出）
開新專案流程為「dirty 偵測 → 跳存檔三選 → 使用者按儲存 → 靜默寫回原檔 → 再跳「另存新檔」」。第二個「另存新檔」對話框本意是「為開新專案挑位置並錨定」，卻緊接在第一個步驟後，使用者易誤解為「把 dirty 另存」；若誤以同檔名存到原路徑，會把原檔覆寫成目標平台的乾淨初始積木（saveFileAs 寫的是 `_getInitialProjectXml`，非工作區內容）。

### 改善內容（方案 2 = 後端防呆 + 語意純化）
1. **Rust 後端防呆**（`src-tauri/src/commands/file.rs` `save_file`）：新增 `SAME_AS_CURRENT` 錯誤——當 `save_as == true` 且目標 path 命中「目前專案檔」時，於寫入前擋下並回錯誤碼（無論是否 examples，先於 examples 檢查）。另存對話框新增 `.set_title()`，開新專案流程（前端傳 `dialogTitle`）顯示「開新專案：選擇儲存位置」。
2. **VSIX 後端防呆**（`src/handlers/fileOps.ts` `handleSaveFileAs`）：`showSaveDialog` 後若 `path.resolve(chosen) === path.resolve(currentFilePath)` → showWarningMessage + return false（不寫檔，維持原工作區）。
3. **語意純化**（`ui/src/app/persistence.js`）：`_confirmSaveBeforeNew` 改回傳 `{ proceed, saved }`（取代純布林）；`startNewProjectFromHome` 在使用者選「儲存」並成功寫回原檔後，於跳出開新專案另存對話框前，以 `CocoyaBridge.alert` 提示「原始專案已儲存，接下來請為新的<平台>專案選擇存放位置與檔名」（i18n `MSG_NEW_PROJECT_SAVED`）。
4. **前端錯誤碼處理**（`ui/src/bridge/tauri.js` `case saveFile/saveFileAs`）：`SAME_AS_CURRENT` → `this.alert(MSG_SAME_AS_CURRENT)` + return false（中止開新、維持原狀態）；另有 `dialogTitle` 於 `tag==='newProject'` 時傳入 Rust。
5. **i18n**：`zh-hant.js`/`en.js` 新增 `MSG_NEW_PROJECT_SAVED` / `MSG_SAME_AS_CURRENT` / `TITLE_NEW_PROJECT`。
6. **文件**：`docs/backend_api_manifest.md` save_file 簽名補 `dialog_title`、Err 碼補 `SAME_AS_CURRENT`。

### 異動檔案
- `src-tauri/src/commands/file.rs`
- `ui/src/app/persistence.js`
- `ui/src/bridge/tauri.js`
- `src/handlers/fileOps.ts`
- `ui/src/zh-hant.js`、`ui/src/en.js`
- `docs/backend_api_manifest.md`

### 驗證
- `node --check` ×4（tauri.js/persistence.js/zh-hant.js/en.js）✓
- `npx tsc --noEmit -p tsconfig.json` ✓
- `npx vite build` ✓ built in 275ms
- `cargo check` ✓ Finished（僅既有 warning：python.rs unused、file.rs:716 dead_code，與本次無關）
- [ ] 實機待測（Tauri + VSIX）：開 examples MCU dirty → 開 PC 新檔 → 儲存 → 應出現「原專案已儲存」提示 → 開新專案另存對話框標題=「開新專案」→ 選到原路徑應被擋下（SAME_AS_CURRENT）+ 原檔內容保留 → 選不同路徑正常開新檔。

### 下次啟動方向 (Next Steps)
- 實機驗證上述三模組（防呆擋下、原檔保留、正常開新）。
- 提醒：Tauri Rust 修改需重建 MSI 才在 Release 生效（dev 模式 cargo tauri dev 即時生效）。
## 追記：「原專案已儲存」提示未等待確認就跳第五步（2026-09-06）

### 問題
實機發現：開新檔流程第四步「原始專案已儲存…」提示對話框跳出後，還沒按確定，第五步的「開新專案另存」對話框就馬上出現（Tauri）。

### 根因（Technical Deep Dive）
`ui/src/bridge/base.js` 的 `alert(message)` 呼叫 `this.send('alert', {message})` 但**沒有 return**。因此 `persistence.js startNewProjectFromHome` 的 `await window.CocoyaBridge.alert(msg)` 實際 await 到 `undefined` → 立即繼續 `send('saveFileAs', ...)` 跳第五步。

Tauri 端 `send('alert')` 內部是真正會阻塞的（`_handleNativeDialogs` → `await _showAlertDialog`，自訂 token 對話框，僅在 OK/Enter/Escape/背景點擊的 `settle()` 才 resolve），只是 `alert()` 沒把這個 promise 還回呼叫端。

### 修正
`base.js` 的 `alert()` 改為 `return this.send('alert', { message });`。修正後 Tauri `await bridge.alert(msg)` 會真正阻塞直到關閉提示。
- 安全面：不影響未 await 的既有呼叫（仍 fire-and-forget）；有 await 的呼叫原本就意圖等待，現才真正生效。其他 `await this.alert(...)` 呼叫點一併受惠。
- 限縮：VSIX `send()` 是 fire-and-forget（postMessage 無回傳值），`alert()` 回傳的是「postMessage 已送出」的 promise，無法等使用者關閉原生 information message；若 VSIX 也出現同類重疊，需改用前端自訂阻塞 modal（如 save-confirm 樣式）於 persistence.js 內直接呈現，不走 bridge。

### 異動檔案
- `ui/src/bridge/base.js`（`alert()` 回傳 send promise）

### 驗證
- `node --check` ×3（base.js/persistence.js/tauri.js）✓
- `npx vite build` ✓ built in 352ms
- [ ] 實機待測（Tauri）：開 examples MCU dirty → 開 PC 新檔 → 儲存 → 應停在「原專案已儲存」提示，按確定後才跳出「開新專案」另存對話框。
## 追記：SAME_AS_CURRENT 提示後自動重試開新專案對話框（2026-09-06）

### 需求
「不能存到目前專案檔的位置」防呆提示出現後，希望自動再跳出「開新專案」另存對話框（第五步），讓使用者改選其他位置，而非就此中止。

### 修改
`ui/src/bridge/tauri.js` `case saveFile/saveFileAs` 原本以 `try/catch` 單次執行；改為 **防呆重試迴圈**（`MAX_SAME_AS_CURRENT_RETRY = 5`）：
- 成功 → `saveCompleted` + `_refreshAnchor` + return true。
- `SAME_AS_CURRENT` → alert 提示「不能存到目前專案檔的位置」後 `continue`，重新 `invoke('save_file')` 彈出另存對話框，讓使用者改選位置（這滿足「提示後自動再跳第五步」）。
- `EXAMPLES_PATH` → 既有 `_handleExamplesSaveDialog`，return done。
- `Canceled` / 其他錯誤 → return false。
- 連續 5 次均命中目前路徑 → alert「多次選到目前專案檔的位置，已中止操作。」+ return false（避免無限彈窗）。

安全性：後端 `save_file` 的 `SAME_AS_CURRENT` 只在 `save_as==true` 且『彈出對話框後』由使用者選到原路徑時才可能觸發；一般 `saveFile`（save_as=false）不彈對話框、不命中，不受影響。重試僅會重新彈出另存對話框，屬合理 UX。

i18n 新增 `MSG_SAME_AS_CURRENT_ABORT`（zh-hant / en）。

### 異動檔案
- `ui/src/bridge/tauri.js`
- `ui/src/zh-hant.js`、`ui/src/en.js`（新增 `MSG_SAME_AS_CURRENT_ABORT`）

### [追加] SAME_AS_CURRENT 分支補 await（2026-09-06）
- 上一輪在 `tauri.js` case saveFile/saveFileAs 的 `SAME_AS_CURRENT` 分支呼叫 `this.alert(...)` 後直接 `continue`，未 await。因 `alert()` 已改為回傳阻塞 promise（Tauri 自訂對話框），若不等待，下一個另存對話框會在提示未關閉時**重疊彈出**。（與先前「原專案已儲存提示未等待」同一類 bug。）
- 修正：`SAME_AS_CURRENT`、`BKY_SAVE_FAILED`、`MSG_SAME_AS_CURRENT_ABORT` 三處 alert 皆補 `await`，確保提示關閉後才重新彈出另存對話框 / 中止。
- 驗證：node --check ✓；vite build ✓（183ms）。

### 異動檔案
- `ui/src/bridge/tauri.js`（追加補 await）

### 驗證
- `node --check` ×3（tauri.js/zh-hant.js/en.js）✓
- `npx vite build` ✓ built in 299ms
- [ ] 實機待測（Tauri）：開新專案 → 另存選到目前專案檔位置 → 提示後應自動再跳出「開新專案」對話框；改選其他位置 → 正常開新檔；連續多次選回原路徑 → 達上限中止。

### 下次啟動方向 (Next Steps)
- 彙整開新專案流程之實機驗證清單：①原專案已儲存提示等待確認；②SAME_AS_CURRENT 擋下 + 自動重試另存；③改選位置後正常開新；④連續命中中止。

## MCU 硬體控制模組一般化（板子感知腳位 SSOT + VID/PID 連動）

### 異動
1. `ui/src/modules/hardware/board_defs.json`（新檔）：開發板資訊表 SSOT。每板含 `name`/`vidPid`/`pins`（ref=label+tags）/`gpioMap`。新增板子只需加一個 boards 條目。
2. `ui/src/module_loader.js`：載入模組時 fetch `modules/<id>/board_defs.json` → `window.CocoyaBoardDefs[moduleId]`（無檔靜默略過）。
3. `ui/src/modules/hardware/hardware_blocks.js`：新增 `CocoyaBoard` 登錄器（IIFE，掛 window）——
   - `mcu_pin_shadow` 改為依目前板子動態組下拉 options（未指定板 = 全板合併 + [板名] 前綴，與舊版相容）。
   - `setCurrent(boardId)` 寫 localStorage（key: cocoya_hw_board）並刷新所有已註冊 pin 積木（field.setOptions）。
   - `resolveGpio(boardId, ref)`：gpioMap 權威映射 → 跨板合併查找 → 舊字串解析 fallback；失敗回 null。
4. `ui/src/modules/hardware/hardware_generators.js`：新增 `cocoyaResolvePinNum()` / `cocoyaPinErrorComment()`；digital_write/read、analog_read、pwm_write 四個產生器改走權威映射，解析失敗輸出 `# [Cocoya] 無法解析腳位: ...` + `pass`（不產壞碼）。
5. `src-tauri/src/commands/mcu.rs`：`SerialPortResult` 加 `board_id`（**`#[serde(rename_all = "camelCase")]`** → 前端讀 `boardId`）；新增 `detect_board_id(vid,pid)`（2E8A:0003→picow、2E8A:0005→maker-pi、303A:*→xiao-s3、未知空字串）；`get_serial_ports` 回填。
6. `ui/src/ui/hardware.js`：updateSerialPorts 記錄埠→boardId map（root.__boardIdMap）；選埠與刷新時 `_applyBoardFromPort()` → `CocoyaBoard.setCurrent()` 自動切板。
7. `src/handlers/serialOps.ts`（VSIX）：由 PNPDeviceID 推導 boardId（boardIdMap，與 JSON SSOT 對齊）併入 ports payload。
8. 文件：`FILE_STRUCTURE.md`、`docs/backend_api_manifest.md`。

### 技術深挖 (Technical Deep Dive)
- **VID/PID 粒度斷層**：原 `detect_board_type` 只到韌體類型（micropython/pybricks），無法區分 Pico/Maker Pi/XIAO 的腳位定義 → 新增細粒度 `detect_board_id`，key 與 board_defs.json 的 boards key 一致。
- **通用橋接晶片限制**：CP210x(10C4)/CH340(1A86) 無法辨識後端 MCU → boardId 留空，前端保留手動選板 fallback（雙軌：自動偵測優先 + 手動覆寫）。
- **serde camelCase 鐵律**：SerialPortResult 加欄位時同步補 `#[serde(rename_all = "camelCase")]`，避免既有 port/label 變 snake_case 之外的 boardId undefined 坑。
- **相容性**：舊 XML 範例寫死 `board.GP0`/`board.D10` 仍可解析（gpioMap 命中或舊字串 fallback）；shadow 積木 init 時 options 為空會 fallback `[['GP0','board.GP0']]`。
- **限制**：Rust detect_board_id 與 VSIX boardIdMap 目前為手動同步 JSON 的 vidPid（程式內註解已標明）；未來可改執行期讀同一份 JSON（已列 todo）。

### 下次啟動方向 (Next Steps)
- Python deploy 端（`resources/deploy/base.py`）讀 board_defs.json vidPid 回傳 boardId，與 Rust/前端同 SSOT（見 todo 待辦）。
- 實機驗證：插板自動切板、下拉只列該板腳位、舊專案 XML 相容、VSIX/Tauri 雙模。

## 追加：手動選板 UI + 嚴格腳位映射（清理舊格式相容）

### 異動
1. `ui/index.html`：序列埠旁新增 `<select id="board-selector">`（手動開發板選擇器，MCU 模式顯示）。
2. `ui/src/style.css`：`.board-select` 樣式（含深色主題）。
3. `ui/src/ui/hardware.js`：
   - `_initBoardSelector()`（updateSerialPorts 首次觸發綁定）：選項 = 自動(依序列埠) + board_defs.json 全部板子；change → CocoyaBoard.setCurrent。
   - `updateBoardSelector()`：重建選項（CocoyaBoard.onChange 自動偵測切板時同步下拉）。
   - `updateSettingsMenu`：僅 MCU 模式顯示 board-selector。
4. `ui/src/zh-hant.js` / `en.js`：新增 TLB_BOARD / TLB_BOARD_AUTO。
5. **嚴格映射清理（無舊 XML 相容）**：
   - `hardware_blocks.js` `resolveGpio`：移除跨板合併查找與 board.GPx/board.Dx/board.LED 字串解析 fallback，只認目前板子 gpioMap，查無回 null。
   - `hardware_generators.js` `cocoyaResolvePinNum`：同步移除字串 fallback。

### 腳位清單性質說明
- board_defs.json 的 pins 為**沿用舊硬編碼的常用子集**（Pico 9 腳 / Maker Pi 9 腳 / XIAO 11 腳），非完整 pinout；tags 欄位（digital/pwm/adc）已預留但**尚未**依積木類型過濾（例如類比讀取積木目前仍列全部腳位）——待辦：依 tags 過濾下拉 + 補齊各板完整腳位。

### 下次啟動方向 (Next Steps)
- 實機驗證：未插板時手動選板 → 下拉只列該板腳位；選「自動」且無埠時維持合併清單。
- 依 tags 過濾（digital_write 只列 digital/pwm 腳、analog_read 只列 adc 腳）。

## 追加：mcu_car 模組遷移至 gpioMap 嚴格映射（maker-pi 專用車，行為不變）

### 決策
mcu_car 為固定 Maker Pi RP2040 接線（GP8-11 馬達、GP12-15 伺服、GP20/21 按鈕為板載），經確認仍遷移至 gpioMap：選 maker-pi 時行為完全不變（GP12→12 等映射一致），選其他板則產生錯誤註解擋下（避免對錯誤板產生錯腳位碼）。

### 異動
1. `ui/src/modules/hardware/board_defs.json`：picow 與 maker-pi 的 gpioMap 由常用子集擴充為 **GP0–GP22 + GP26–28 全腳**（RP2040 GPIO 編號同源，兩板共用）。
2. `ui/src/modules/mcu_car/mcu_car_generators.js`：新增 `mcuCarResolvePin()` / `mcuCarPinError()`（與 hardware 模組同構），替換 8 處 `replace('board.GP','')` 舊解析——servo、servo_setup、ultrasonic（TRIG/ECHO）、check_color、check_gray、wait_start、button_pressed。
3. 板載馬達類別內的 GP8-11 硬編碼屬固定接線，保留不動。

### 全域掃描結論
`ui/src` 內已無任何 `replace('board...')` 舊字串解析；`board.GPx` 字串僅存於 board_defs.json（SSOT 資料）、各 blocks 的下拉 value（資料，由 gpioMap 解析）與 toolbox.xml 預設值（GP0/GP1/GP2/GP26，RP2040 板可映射；選 XIAO 時該預設會產錯誤註解，屬預期防護）。

### 驗證
node --check + JSON parse + vite build 全綠。

## 追加修正：未選板時 GPIO 積木全部無法解析（examples/test.xml 重現）

### 原因
嚴格模式下 resolveGpio 需有「目前板子」才能查 gpioMap；使用者未插板（自動+空埠）時 currentBoardId 為空 → 所有 GPIO 積木回 null → 全部產生「無法解析腳位」註解。

### 修正
1. `hardware_blocks.js` `resolveGpio`：**有選板 → 只查該板 gpioMap**（權威，查無回 null）；**未選板 → 依序掃描所有板的 gpioMap 取第一個命中**（資料查找，非恢復舊字串格式解析；GPx 同源板結果一致，Dx 命中 xiao-s3）。
2. `board_defs.json`：picow/maker-pi gpioMap 補 `"LED": 25`（Pico W 板載 LED，相容舊 `board.LED` ref）。

### 確認
examples/test.xml 使用 board.GP0/GP1/GP2/GP26 → 未選板經掃描全部可解析；選 maker-pi 後行為一致。

### 驗證
node --check + JSON parse + vite build 全綠。

## 追加：board_init 宣告積木架構（工作區板子 SSOT）

### 設計定案（與使用者討論）
- **board_init 積木為板子宣告 SSOT**：寫進 XML 隨專案保存，「插 A 板開 B 程式」不再互相干擾。
- **開新 MCU 專案自動塞 board_init**（預設 maker-pi）。
- **移除 toolbar 手動選板下拉**（含 style/i18n），板子狀態只由工作區 board_init 驅動。
- **上傳比對**：board_init 宣告 vs VID/PID 偵測不符 → confirm 攔截（可仍要上傳）；無 board_init → 不攔截（編輯有合併掃描兜底）。
- **πCar/AI 等全部 MicroPython 範例 XML（28 檔）批次塞入 board_init(maker-pi)**；test.xml 保留無宣告（驗證兜底）。

### 異動
1. `ui/src/modules/hardware/board_defs.js`（新，取代 JSON 為 SSOT）：loadScript 載入（與模組腳本同機制，**消除 fetch/CSP/時序風險**——test.xml 全無法解析的根因即 fetch 失敗）；JSON 已刪。
2. `ui/src/module_loader.js`：fetch → loadScript。
3. `hardware_blocks.js`：新增 `mcu_board_init`（帽子型宣告積木，field validator → setCurrent(id,'workspace')）；`resolveGpio` 查無改為**無條件合併掃描兜底**；CocoyaBoard 移除 localStorage（狀態 = 工作區宣告）。
4. `hardware_generators.js`：mcu_board_init 產生 `# Board: xxx (id)` 註解。
5. `hardware/toolbox.xml`：board_init 置頂。
6. `hardware/i18n/{zh-hant,en}.js`：HW_BOARD_INIT / HW_BOARD_INIT_TOOLTIP。
7. `ui/src/app/lifecycle.js` createDefaultBlocks：MicroPython 初始工作區加入 board_init（maker-pi）。
8. `ui/src/ui/base.js`：bind onclick 改 async；MCU 上傳前比對宣告/偵測（MSG_BOARD_MISMATCH confirm）。
9. `ui/index.html`/`style.css`/`ui.js`/`zh-hant.js`/`en.js`：移除 toolbar board-selector 與 TLB_BOARD*。
10. 範例 XML 28 檔批次插入 board_init；FILE_STRUCTURE/mcu.rs/serialOps.ts 註解指向 board_defs.js。

### 優先權（最終）
工作區 board_init 宣告 > （無宣告時）合併掃描兜底；VID/PID 只用於上傳比對，不再驅動編輯狀態。

### 驗證
node --check ×9 + vite build + cargo check 全綠；範例 XML [xml] 解析驗證 OK。

### 下次啟動方向 (Next Steps)
- 實機驗證：① 開新 MCU 專案看到 board_init(maker-pi)；② 開 test.xml 無宣告 → GPIO 可解析；③ 改 board_init 下拉 → pin 積木即時刷新；④ 插板與宣告不符 → confirm 攔截；⑤ πCar 範例開檔即 maker-pi。
- Rust/VSIX vidPid 表仍手動同步 board_defs.js（未來執行期讀取或 codegen）。

## 修正：範例 XML board_init 插入位置違反 Blockly variables 前置規則
- 錯誤：loadWorkspace 報「''variables'' tag must exist once before block and shadow tag elements」——批次插入把 board_init 放在 <variables> 之前。
- 修正：28 檔重插——移除原位置，改插在 </variables> 之後（無 variables 的檔插在根標籤後）；腳本驗證所有檔 board_init 均在 <variables> 之後（ORDER_OK），抽 3 檔 [xml] 解析通過。
