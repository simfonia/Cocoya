# 2026-09-11 工作日誌

## 任務：#task[dataset manager流程改造規畫-M2] 文件補齊＋R4/R5/R6（Act）

### 文件補齊
- 備份 `backup/DatasetManagerTypeLockedWorkflow_20260911_120000.bak`、`FILE_STRUCTURE_20260911_120000.bak`、`ui_layout/entryCards/ui_components_pre_R4`。
- 計畫 `log/plan/DatasetManagerTypeLockedWorkflow.md`：檔頭加 2026-09-11 更新行；新增 `## 9. M1 後續熱修與導航改造`（熱修1/2+4/3/5系＋P1/P2/P3＋5 新 key＋M2 鐵律）；§6 R4/R5/R6 標完成。
- `FILE_STRUCTURE.md`：core 補 typePolicy/html 行；application 補 sessionManager 行；ui 補 entryCards/labelManager/samplerPanel 行；ui_layout 行改 M1＋導航註記；i18n 118→142 key；ui_components 行改轉義經 core/html.js。

### R4：normalizePath 去重＋core/html.js
- 新建 `core/html.js`（`escapeHtml/escapeAttr`）＋`html.test.mjs`（5 測試）。
- `ui_components.js`：刪除損壞的本地 `escapeHTML`（`replace(/&/g,'&')` 等同 no-op，XSS 防護失效），改 `import { escapeHtml as escapeHTML } from './core/html.js'`。
- `ui/entryCards.js`：刪 `esc()` 改 import。
- `ui_layout.js`：刪 `escapeHtml`/`normalizePath` 雙函式，改吃 `core/html.js` 與 `pathPolicy.normalizePath`（SSOT）；清 `getTypeEntry` 未使用 import。
- 驗證：86/86＋check×4＋vite PASS；備份 `backup/*pre_R4*`。

### R5：ui/labelManager.js
- `ui_layout.js:681~800` 約120行 `createLabelMapManager` 原樣搬移為 `createLabelManager({...全注入...})`（bridge/document/UICanvas/回呼注入；`document.getElementById`→`doc()`）；layout 僅留委派 wrapper。
- `annotation.js`/`classification.js` 的 `createLabelMapManager` 依賴簽名不變，直接沿用新模組，無需改碼。
- layout 清掉 `countBoxesWithClassId/countImagesWithLabel/removeAnnotationsByClassId/reassignLabelsToUnlabeled` 未使用 import（僅留 `countUnclassifiedBoxes`）；附 `labelManager.test.mjs`（2 測試：空容器早退／新增寫入 label_map）。
- 驗證：88/88＋check×4＋vite PASS；備份 `backup/*pre_R5*`。

### R6：ui/samplerPanel.js
- `refreshDynamicPanels` live 段約60行抽為 `createSamplerPanel({setupLiveSamplerView/handleSamplerLabelChange})`；layout 僅留委派；相機開關語意（DOM 真值＋`stopCamera(true)`）原樣保留於 `renderSamplerView`。
- 修測試假 DOM：`handleSamplerLabelChange` 改 `modal?.querySelector?.` 防禦，移除多餘 `getDocument` 依賴。
- 附 `samplerPanel.test.mjs`（3 測試：空標籤列舉／預選首標籤／新標籤補 map＋刷新）。
- 驗證：91/91＋check×2＋vite PASS；備份 `backup/*pre_R6*`。

### 熱修6：清除資料誤跳 table 模式（2026-09-11 實機回報）
- 症狀：P2 工作區按右上「清除資料」，執行後類型跳回 table，非原進入類型。
- 根因：M1 前 `clearBtn.onclick` 寫死 `typeSelect.value='table'`＋`updateSourceModeOptions('table')`＋`createDefault({type:'table'})`；M1 類型鎖定後未同步改，造成鎖定被覆寫。
- 修正：`ui_layout.js` 清除流程改以 `sessionManager.snapshot().lockedType || getFormValue('projectType') || 'table'` 為準，三處（下拉值／modes／spec）一致保留；下拉仍 disabled。
- 驗證：`node --check`＋91/91＋vite PASS。待實機：image/object_detection 進入→清除→仍為原類型＋副標題徽章不變。備份 `backup/ui_layout_pre_clearfix_20260911_133000.bak`。

### 技術深挖（Technical Deep Dive）
- `ui_components.js` 舊 `escapeHTML` 五個 replace 右值皆為無轉義字面量（`'&'/'<'/'>'/'"'`），僅 `'&#039;'` 有效；任何含 `&<>` 的標籤名會原樣注入 innerHTML。R4 以 `core/html.js` 正確五元轉義取代，全站 `escapeHtml/escapeHTML` 唯一出口。
- `normalizePath` 雙版本語意差：layout 用 `??`（保留 `0/false`），pathPolicy 用 `||`（`''`→`''` 同結果）；比對場景皆為字串路徑，收斂無行為差。
- R5/R6 採「原樣搬移＋委派」策略：annotation/classification/sampler 的 DOM 契約與相機競態修正（熱修2/4）零改動，僅改依賴來源；遞迴 `reRender` 內的 `createLabelMapManager` 自指改為模組內自指，語意不變。
- 清除修正取 `lockedType` 優先於 `getFormValue`：防下拉 DOM 已被污染時仍以 session 為準；`allowedModes(lockedType)[0]` 保 mode 合法。

### R7：表格 samples 落盤（前 2000＋truncated）
- `spec.js`：新增 `export const TABLE_SAMPLES_PERSIST_LIMIT = 2000`＋static `buildTableSamples(tableRows, limit=2000)`（純函式回傳 `{samples, truncated, total}`）；`normalizeStats` 增 `samples_truncated`（`=== true`，舊檔無→false）；`createDefault` 增 `samples_truncated:false`。
- `ui_layout.js syncSpecFromUI`：非影像系（table/feature/serial）落盤 samples 由舊「一律 `[]`」改委派 `DatasetSpec.buildTableSamples`——samples 前 2000 筆、`stats.sample_count` 記全量 total、`stats.samples_truncated` 表露截斷；影像系不變。`includeSamples=false` 仍沿用舊 samples（預覽/防抖路徑不受影響）。
- 新增 `spec.test.mjs`（6 測：LIMIT=2000、buildTableSamples 未超限/超限截斷/非陣列+自訂 limit、createDefault 旗標、toJSON round-trip preserve）。
- 後端零改：`datasetSaveProgress/datasetExport` 皆以 `JSON.stringify(spec)` 透傳 `stats.samples_truncated`，不解讀不攔截；Rust/TS 無 struct 欄位需動。
- 驗證：**97/97**＋check×3＋vite PASS。備份 `backup/*pre_R7_20260911_134500.bak`（spec/ui_layout/progressUseCases）。
- 已知限制（記入 Next Steps）：表格類載入套回 `applyLoadedProgress` 仍只依 `image_path` 套回 images[]，不重建 tableRows；表格 resume 的 rows 恢復屬 M3 表格模板範圍。

### 下次啟動方向（Next Steps）
1. R8 匯出分流＋serial 擋下（新 `DSM_*` key＋parity；table 分支待 M-T1）。
2. R9 後端對齊＋雙平台實機（每類卡→徽章→匯出/存讀：image/od/line 走影像、table 走 CSV/JSON、feature/serial 擋下；三主題目視）。
3. M3 表格＋循線模板；表格 resume 的 tableRows 套回（R7 已知限制）。

### R8：匯出按類型分流（serial/dev 擋下）
- `exportUseCases.js`：`needsAnnotationCheck/needsUnclassifiedCheck` 改吃 typePolicy（去除手寫字面比較）；新增隊首 `isDevType(projectType)` 擋下 feature/serial——`showExportProgress(false)`＋`showStatusMessage(ERROR_EXPORT_DEV_UNAVAILABLE)` 後 return，不觸發後端 request。
- 新增 i18n key `DSM_ERROR_EXPORT_DEV_UNAVAILABLE`（zh/en 各一）；parity **147/147** 一致（僅預期內動態前綴 `ENTRY_TYPE_/ENTRY_DESC_` 與測試 `KEY` 為提示非缺漏）。
- 新增 `exportUseCases.test.mjs`（2 測：feature/serial 擋下且不 call request＋sync=0；table 非 dev 不在擋下段早退）。
- 本次僅檔下「匯出」；資料可繼續採集/存讀（進 feature/serial 的 samples 仍經 R7 落盤），匯出按類型完全分流（image/od/line→影像 ZIP、table→CSV/JSON）留待 M3 表格模板＋後端 export 分支。
- 驗證：**99/99**＋check×4＋parity 147/147＋vite PASS。備份 `backup/*pre_R8_20260911_141500.bak`（exportUseCases/zh-hant/en）。

### R9：後端對齊＋雙平台實機
- **後端對齊（本輪完成，零改動）**：R4–R8 皆前端 `ui/src` SSOT；後端 command 以 `spec_json`/`JSON.stringify(spec)` 透傳 `project.type`／`stats.samples_truncated`，無 Rust struct/TS handler 欄位需動，亦無需同步 sidecar dev 副本（未改 `resources/` 的 .py）。驗證：`cargo check`（3 既有 dead_code warning，非新）、`cargo test` 1/0、`tsc --noEmit -p tsconfig.json`、`npm run compile`（VSIX tsc）、`vite build` 全綠。
- **雙平台實機（使用者 backlog，環境無法跑 GUI）**：VSIX＋Tauri 各跑「每類卡→徽章→選定鎖定→匯出/存讀一次」；特別驗證 feature/serial 按匯出出現 `ERROR_EXPORT_DEV_UNAVAILABLE`、image 清除後**仍鎖定原類型**（熱修6 回歸）；三主題（light/dark/candy）目視 entry 卡片＋dev banner＋類型徽章。
- **後續**：M3 表格＋循線模板（M-T1/M-L1）、M4 特徵最小可用（M-F1）；M3 才落實「table 匯出寫 independent CSV」＋表格 resume 套回 tableRows（R7 已知限制）。

### 下次啟動方向（Next Steps）
1. 雙平台實機驗證（R9 未竟 backlog，需使用者執行 GUI）。
2. M3 表格＋循線模板（M-T1/M-L1）；M4 特徵最小可用（M-F1）。
3. 「table 匯出寫 independent CSV」＋載入套回 tableRows（R7/R8 已知限制）。
