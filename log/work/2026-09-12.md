# 2026-09-12 工作日誌

## 任務：#task[dataset manager流程改造規畫-M3] 表格＋循線模板跑通（M-T1/M-L1，Act）

### M3 完成總覽
- T-1 `common/table_dataset.py`：label 欄 type 判定→分類（分層＋教學保護）／回歸（隨機切＋報告註明）
- T-2 `table/table_train.py`：MLP（分類 softmax／回歸 linear）＋TFLite＋labels.txt＋報告＋RESULT 契約
- T-3 sidecar 匯出 table 分流（→`data.csv`，UTF-8；samples_truncated 警告）
- T-4 sidecar 訓練映射：`trainLocal` task→檔名（修硬編碼＋`--model_type` 誤參）；遠端 script_rel 對齊實檔（修幽靈 `object_detection/`）
- L-1 sidecar 匯出 line 分流（→`lines/` 同名 .txt，`x1 y1 x2 y2`；未標註跳過＋>50% 警告）
- L-2 `common/line_dataset.py`：images/＋lines/；回歸隨機切＋報告註明；<2 樣本報錯
- L-3 `line_follower/line_follower_train.py`：重用 detector `Dense(4,sigmoid)` 回歸頭（語意為端點）；MSE＋報告
- 實機：table 分類/回歸訓練跑通＋**f32/int8 TFLite**＋labels.txt；line 訓練跑通＋f32；sidecar exportDataset 真實行程 e2e 16/16 全 PASS
- 驗證：`py_compile` 5 檔 PASS（含 sidecar）＋全命令列煙霧（`temp_scripts/m3_smoke_make_data.py` 合成資料）
- 日誌：`log/todo.md` M3 更新；`FILE_STRUCTURE.md` 訓練模板＋sidecar 行更新；備份 `backup/dataset_sidecar_pre_M3_20260911_170000.py`
- 計畫文件待同步（L-1 由 lines.txt 改 lines/ 目錄；T-4/T-5 細節）——下輪整入 §10

### 技術深挖（Technical Deep Dive）
- sidecar export 契約：staging 的 dataset.json 是唯一真相——匯出分支一律讀 `sourceDir/dataset.json`（`spec`），不依賴請求方多帶的 payload；後端零 command 改動（R9 契約維持）。
- lines/ 目錄勝過單一 lines.txt：每張標註影像同名 .txt 可與 images/ 逐一配對，避免行列錯位；與 YOLO labels/ 慣例對稱；line_dataset 掃描模式與 detector_dataset 同構。
- table int8 量化：MLP MLP 量化比約 68%（11.1→7.6KB）；代表集直接取訓練 ds 前 100 batch——小模型上本機數十秒可驗。
- `trainLocal` 舊鏈：硬編碼 classifier＋`--model_type` → 三腳本 argparse 全都 unrecognized（exit 2）；修為 task→檔名映射＋`--backbone`。

### 施工事故
- editor 分段寫入時工具結果遺失導致兩次重複/截斷插入（table_train `save_table_history` 尾、`generate_table_report` 中段、檔尾孤兒片段、line_train 訓練呼叫被覆蓋）——逐一以精準 old/new 修復＋`py_compile` 驗收；後續大檔一律 4 段以內＋每段後 compile。
- 背景 TF 訓練逾越單命令 30s 上限兩次——改 `Start-Process`＋log 檔非同步輪詢。

### 下次啟動方向（Next Steps）
1. 計畫文件 §10 同步（L-1 lines/、T-4 修復清單、驗證結果）。
2. M4 特徵最小可用（M-F1）；M5 文件＋清理。
3. 使用者 backlog：VSIX+Tauri 雙平台 GUI 實機（DM 匯出→積木訓練整鏈）。
---

## [追加 2026-09-12] M4 計畫定稿（特徵最小可用 M-F1）
- 建立計畫檔 `log/plan/DatasetManagerFeatureMinimalM4.md`（Phase 0~5 分階段施工計畫＋錨點盤點＋風險評估＋驗證計畫＋三決策待拍板）。
- 更新 `log/todo.md` M4 行：附計畫檔指標＋Phase 0~5 子項＋決策待辦。
- 關鍵發現（先記，詳見計畫 §2）：
  - sidecar `trainLocal`（L843-848）`task_scripts` 對 feature 誤落 `classifier_train.py`（會壞）。
  - sidecar `trainRemote`（L610-611）`feature → feature/feature_train.py` 為幽靈檔（不存在）——同 M3 修過的 `object_detection/` 坑。
  - `media_pipe_service.py` 僅歷史 stub（僅手部「偵測」、無 landmark 提取），需重寫 Hand21＋Pose33。
  - `config/python_modules.json` 已有 `mediapipe` 條目，缺裝判斷可複用環境診斷 status。
- 施工未啟動：等三決策（採集範圍／點維度 xy 或 xyz／訓練檔新建或映射）拍板＋Act mode。
