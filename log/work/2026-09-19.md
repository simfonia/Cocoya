# 2026-09-19 工作日誌

## #task[依開發板更新腳位資訊] Maker Pi RP2040 pin map 依官方 Datasheet 更新

### 需求與資料來源
使用者提供 Cytron 官方文件（Google Docs，內容＝<em>MAKER-PI-RP2040 Datasheet Rev 1.2 (Jan 2022)</em>），要求「根據此文件更新 Cocoya 中 Maker Pi RP2040 的 pin map」。
以 `https://docs.google.com/document/d/<id>/export?format=txt` 取得純文字全文（edit 頁面為 JS 殼，抓不到內容）。

### 從 datasheet Table 1 萃取的事實
| 類別 | 對照 |
|---|---|
| 直流馬達 | M1A=GP8、M1B=GP9、M2A=GP10、M2B=GP11（Truth Table 亦以 GP8/GP10=Input A、GP9/GP11=Input B） |
| RC 伺服 | 4 組訊號 GP12~GP15（V+ = 電源電壓） |
| RGB LED | WS2812B 接 GP18 |
| 可程式按鈕 | GP20、GP21 |
| Piezo 蜂鳴器 | GP22 |
| Grove 埠 | 1=GP0/GP1、2=GP2/GP3、3=GP4/GP5、4=GP16/GP17、5=GP6/GP26(A0)、6=GP26(A0)/GP27(A1)、7=GP7/GP28(A2) |
| Analog | GP26=ADC0、GP27=ADC1、GP28=ADC2（其餘腳位 Analog 欄為空） |
| USB VID:PID | 2E8A:1000（CircuitPython / Arduino Core）；Cocoya 用 MicroPython 為 2E8A:0005 |

注：原始表中 GP26 同時列於 Grove 5 與 Grove 6（依原文登載，未自行更正，已在檔案註解與文件標明）。

### 異動清單
1. `ui/src/modules/hardware/board_defs.js`（maker-pi）
   - `pins` 由 9 項擴充為 **25 項**：板載週邊（GP8~GP11 馬達、GP12~GP15 伺服、GP18 RGB、GP20/GP21 按鈕、GP22 蜂鳴器）
     ＋ Grove 埠全數（GP0~GP7、GP16、GP17、GP26~GP28）。
   - `label` 加功能提示（`GP8 (M1A)`、`GP12 (Servo)`、`GP18 (RGB LED)`、`GP20 (Button 1)`、`GP26 (Grove 5/6, ADC0)`…）——label 僅為顯示，`ref` 不變 → 舊專案不受影響。
   - `tags` 依 datasheet 的 PWM/SPI/I2C/UART/Analog 欄補齊（詞彙擴充：`digital/pwm/adc/input/i2c/uart/spi`）。
   - 檔頭註解補上 tags 詞彙與「label 顯示 / ref 解析鍵」分工說明。
2. `ui/src/modules/hardware/board_defs.js`（microbit，順帶修 bug，見下）
   - `gpioMap` 由僅有 `P0..P20` 擴充為 `PN` 與 `pinN` 雙鍵，並加註解說明。
3. `docs/help/hardware_pins_zh-hant.html` / `hardware_pins_en.html`
   - Maker Pi 段依 datasheet 重寫：新增「板載功能表」與「Grove 埠複用功能表」，移除已過時的「請選擇 `[Pico]` 開頭的選項」提示（改為「先放 `初始化開發板` 積木並選 Maker Pi RP2040」），補 ADC/狀態 LED/電源說明；頁尾資料來源加註 Cytron datasheet。
4. `ui/src/modules/hardware/board_defs.test.mjs`（新，7 測）——資料契約／不變式測試。
5. `log/mappings/HardwarePinMap.html`（新）——開發板腳位 SSOT 對照表（結構、解析鏈、各板一覽、Maker Pi 完整表、新增板子檢查清單、歷史踩坑）。

### 額外發現並修正的既有 bug（micro:bit 腳位全部 miss）
驗證新 maker-pi 資料時，順手寫的「所有板 `pins[].ref` 必可解析」檢查**當場抓到 micro:bit 全數失敗**：

- `pins[].ref` 為 `board.pin0`…（MicroPython `Pin(n)` 語意），但 `gpioMap` 鍵為 `P0`…（大寫）。
- `normalizePinRef('board.pin0')` → `pin0`；查 `gpioMap['pin0']` → miss；全板兜底掃描亦 miss → 選了 micro:bit 的腳位一律產生「無法解析腳位」。
- 修法：`gpioMap` 同時提供 `P0` 與 `pin0` 兩種鍵（19 組），舊 XML 若寫 `board.P0` 也能解析，零破壞。
- 此 bug 與 2026-09-18 的 `scrub_` 標記污染**症狀相同、根因不同**（一個是字串被污染、一個是鍵值大小寫不一致），因此本次把「不變式測試」制度化，未來新增板子會立刻擋下。

### 驗證
- `node --check ui/src/modules/hardware/board_defs.js` PASS。
- 資料不變式驗證（`%TEMP%\cocoya_pinmap_verify.js`，Node + stub 載入真實模組）：
  - `picow 9 / maker-pi 25 / xiao-s3 11 / microbit 19 / spike-prime 0` 塊板**全部 pins 可解析**（修 microbit 前為 19 個 UNRESOLVED）。
  - maker-pi 25 個關鍵腳位號碼全數正確；`pinOptions('maker-pi')` 回傳 25 項，前幾項為 `GP8 (M1A)`, `GP9 (M1B)`, `GP10 (M2A)`, `GP11 (M2B)`, `GP12 (Servo)`…
  - 總結 `ALL PASS`。
- `cd ui; node --test "src/modules/hardware/*.test.mjs"` → **16/16 PASS**（含新 `board_defs.test.mjs` 7 測）。
- DM 回歸 `node --test "src/modules/dataset_manager/..."` → **141/141 PASS**。
- `npx vite build`（ui）PASS。
- 備份：`backup/board_defs_20260919011218.js`、`backup/hardware_pins_zh-hant_20260919*.html`、`backup/hardware_pins_en_20260919*.html`。

### 技術深挖 (Technical Deep Dive)

#### 1. 為什麼 pin map 這種「純資料」也會壞？——兩個獨立的資料／管線契約
本日與昨日兩個 bug 說明同一症狀有兩條不同的失效路徑：
1. **管線污染**（09-18）：`valueToCode()` 回傳值在產生期間帶著 `\u0001ID:...\u0002`，資料本身正確但字串被污。
2. **資料鍵不一致**（09-19）：`pins[].ref` 的正規化結果與 `gpioMap` 的鍵不同（`pin0` vs `P0`）。
兩者都在同一個入口 `resolveGpio()` 收斂（先 `normalizePinRef` 再查表），所以修正點集中、風險低。
**制度化解法**：把「所有板的所有 ref 必須可解析」寫成測試（不變式），資料錯誤在 CI/本機測試即現形，不必等實機。

#### 2. 為什麼標籤（label）可以自由改，而 ref 不行
`ref` 是舊專案 XML（`mcu_pin_shadow` 的 `field` 值）與 `gpioMap` 的查表鍵；`label` 只是下拉顯示文字。
因此本次把「教學價值」（`GP8 (M1A)`、`GP12 (Servo)`）加在 `label`，`ref` 完全不動 → 所有既有專案行為不變、零遷移成本。
未來若要做 i18n 的下拉文字，應改在 `pinOptions()` 以 boardId+ref 查 i18n key，而不是把中文寫進資料層（本輪刻意維持資料層語言的語言中立性：使用 M1A/Servo/Grove 等業界通用詞）。

#### 3. `tags` 的定位（預留欄位）
`tags` 目前無人消費（todo 仍列「依 tags 過濾腳位下拉」為待辦）。本次依 datasheet 把 `i2c/uart/spi` 也標上，
未來實作過濾時可直接 `tags.includes('adc')` 等；若屆時採白名單語意，需注意「加 tag 只會增加選項」的相容性方向（寧可寬鬆、不可缺漏）。

#### 4. 為何 datasheet 的 GP26 重複出現要照登
Grove 5（GP6/GP26）與 Grove 6（GP26/GP27）在原文共用 GP26。硬體上 GP26 是 ADC0 且被拉出到多個埠的 A0 位置是常見設計；
但 Cocoya 的 `pins` 是下拉清單，**同一 ref 不可重複**，故 GP26 僅登錄一次，label 記為 `GP26 (Grove 5/6, ADC0)` 保留雙埠資訊。

#### 5. 抓取 Google Docs 的正確姿勢
`/edit?tab=t.0` 回傳的是 JS 殼（標題可見、內容不可見）；`/export?format=txt` 可直接取得純文字（本文件約 6.3KB），
是本機除錯／規格萃取的可重用技巧（已在 `HardwarePinMap.html` 註明資料來源與版本）。

### 下次啟動方向 (Next Steps)
- 實機（使用者當前為 Tauri dev）：重啟後放 `初始化開發板（Maker Pi RP2040）`，檢查腳位下拉是否列出 25 項並帶 `(M1A)`/`(Servo)` 等提示；抽驗 GP8 馬達、GP12 伺服、GP26 類比讀取產生的 GPIO 號碼。
- backlog（既有）：依 `tags` 過濾腳位下拉（`digital_write`→digital/pwm、`analog_read`→adc）。
- backlog（本日新增）：datasheet 的 CircuitPython/Arduino VID:PID `2E8A:1000` 是否納入自動偵測——需 `board_defs.js` + `mcu.rs` + `serialOps.ts` 三處同步並確認不會與其他 RP2040 板（例如 Pico 的 CircuitPython）誤判。
- 待確認：micro:bit 使用者專案若曾以 `board.P0` 撰寫，本次雙鍵設計已相容（無需遷移）。