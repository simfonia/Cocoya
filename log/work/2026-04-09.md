
## [2026-04-09] 雙模架構重構 - VSIX 穩定性修復與 UI 強化

### 1. 資源路徑與通訊層
- **路徑統一**：將圖示資源由 ui/icons 搬移至 ui/src/icons，以對齊積木 JS 中 mediaUri + '/icons/...' 的動態拼接邏輯。
- **VSIX 映射**：修正 extension.ts 中的 Regex 與路徑處理，確保 ui/src 下的資源能正確被 Webview URI 轉換。
- **通訊抽象**：完成 module_loader.js 與 main.js 對 CocoyaBridge 的切換，移除對原生 scode 物件的直接依賴。

### 2. 初始化與 Bug 修復
- **作用域修復**：修正 main.js 中 CocoyaApp 內部方法的引用錯誤（	his vs CocoyaApp）。
- **載入順序**：在 index.html 中補齊遺漏的 utils.js 與 ui_manager.js，確保全域物件在 main.js 執行前就緒。
- **Minimap 加固**：實作事件鏡像（Mirror）防護，攔截積木 ID 不一致導致的崩潰，並補回 Minimap 切換按鈕與圖示。

### 3. 積木搜尋功能優化
- **UI 強化**：搜尋框加入取消鈕（X）與 ESC 鍵快速取消支援。
- **定位校正**：將搜尋框寬度固定為 100px，並在搜尋時強制獲取 locklyToolboxDiv 的實體 offsetWidth 來設定 Flyout 的 X 座標，解決 Flyout 遮擋 Toolbox 的問題。
- **風格調整**：搜尋框背景恢復為實心白色，提升在各類主題下的可讀性。

### 4. 其它功能
- **Python 縮排**：修正縮排變更後程式碼預覽未同步更新的 Bug。
- **結構文件**：更新 FILE_STRUCTURE.md 以符合 SSOT（單一事實來源）目錄設計。

