# Cocoya 工作日誌 (2026-04-18)

## 任務摘要
- **修復 Python 檢查邏輯**：確保 MCU 模式下也能彈出 Python 路徑選擇視窗。
- **修復範例檔積木 ID**：將所有範例檔中的 `py_io_sleep` 替換為 `py_time_sleep`。
- **強化 MCU 部署韌性**：徹底解決 Windows 下 `CIRCUITPY` 磁碟寫入 Errno 22 (磁碟損毀/鎖定) 問題。

## 詳細異動
1. **src/extension.ts**:
    - 在 `handleRunCode` 的 MCU 分支新增 Python 路徑檢查與選擇流程。
2. **examples/*.xml**:
    - 批次將 `py_io_sleep` 替換為 `py_time_sleep`，對齊最新模組規範。
3. **resources/deploy_mcu.py**:
    - **重構寫入邏輯**：捨棄 Python `open()`，改用 `subprocess` 呼叫 Windows 原生 `cmd /c copy` 指令。
    - **新增內容校驗**：寫入後立即讀取並比對內容，確保部署百分之百成功。
    - **效能優化**：縮短 `monitor` 重連時間至 0.2s，提高捕捉初始 `print` 文字的機率。
    - **異常處理**：自動處理磁碟唯讀、損毀與路徑空格轉義問題。

## 待辦事項
- 繼續進行 Milestone v5.0 的 Tauri 獨立 App 開發。
