# 2026-08-28 工作日誌

## [規劃完成] Remote Training Refactor — 遠端訓練重構（S1 起）

- 計畫文件：`log/plan/RemoteTrainingRefactor.md`（SSOT，跨 agent 交接依據）。
- 前提：Dataset Manager Refactor - Stage 1 已 commit `250f296`，工作樹 clean。
- 決策 D1..D11 已收斂並寫入計畫（雲端燈號移除、DM 上傳鈕移除、host 導向、sync dropdown、TFLite 本地轉、移除 auto 等）。

## [S1] 移除 DM 雲端 ZIP 上傳按鈕 + 前端流程（D2）

- 目標：移除 `#dataset-cloud-upload-btn` / `#dataset-cloud-zip-input` 及分塊上傳前端流程，保留 `datasetExport` 匯出。
- 範圍：`ui/src/modules/dataset_manager/ui_layout.js`
  - DOM：移除上傳按鈕 + zip input。
  - 事件綁定：移除 `cloudUploadBtn.onclick` + `cloudZipInput.onchange`（分塊 Base64 → `datasetUploadArchive`）。
  - `refreshDynamicPanels`：移除 `cloudUploadBtn` 顯示控制，保留診斷區（`dataset-cloud-diagnostic-area` 留待 S5 SSH 精靈合一處理）。
  - `ui_layout.js` 的 `datasetUploadResult` 分支改為「無來源上傳結果已忽略」空分支（原參照已移除之 `cloudZipInput`）。
  - i18n（zh-hant/en）：移除 `STATUS_UPLOADING_ZIP`/`STATUS_UPLOADING`/`STATUS_DECOMPRESSING`/`SUCCESS_UPLOAD`/`ERROR_UPLOAD_FAILED` 鍵；新增 `ERROR_UPLOAD_RESULT_IGNORED`。
- **驗證**：node --check（ui_layout + i18n×2）PASS；`npm.cmd run build --prefix ui` PASS（built in 730ms）。
- **狀態：S1 完成 ✅**（G1 前半達成）。
- **狀態：S1 完成 ✅**（G1 前半達成）。

## [S2] 移除 toolbar 雲端開關 + 全域 cloud-ai 狀態（D1）

- 範圍與異動：
  - `ui/index.html`：移除 AI 下拉的 `#btn-cloud-ai-toggle` 項目、燈號 `#cloud-ai-status`、隱藏舊 `#cloud-ai-toggle`。
  - `ui/src/style.css`：移除對應燈號/隱藏樣式。
  - `ui/src/ui/base.js`：移除 `ensureSshConfig` 開關路徑的 toggle 綁定、`syncCloudAiToggle`（方案C硬關）、`updateCloudAiToggle`、localStorage key（`cocoya_remote_mode_enabled`/`cocoya_cloud_ai_enabled`）讀寫。
  - `ui/src/app/controller.js`：移除 `cloudAiModeStatus` 回寫分支。
  - `ui/src/bridge/base.js` + `tauri.js`：移除 `setCloudAiMode`/`getCloudAiMode` Promise 方法與 case（Tauri not handled 警告一併消失）。
  - VSIX：`src/handlers/envOps.ts` 移除 `handleSetCloudAiMode`/`handleGetCloudAiMode`；`src/cocoyaManager.ts` 移除 dispatch 與 `cloudAiEnabled` globalState 讀寫。
  - Tauri：`commands/app.rs` 移除 `set_cloud_ai_mode`/`get_cloud_ai_mode`（並清掉因此 unused 的 `use std::fs;`）；`state.rs` 移除 `cloud_ai_enabled`；`lib.rs` 移除初始化與註冊；`permissions/commands.toml` 移除兩條權限。
  - 孤兒 i18n：`ui/src/zh-hant.js`/`en.js` 移除 `MSG_CLOUD_AI_REQUIRES_REMOTE`。
- **驗證**：node --check 全 PASS；vite build PASS；`npx tsc --noEmit` + `npm run compile` PASS；`cargo check` PASS（殘餘 3 warnings 皆為既有非本次造成）；全域 grep（cloudAiEnabled / cloud-ai / setCloudAiMode / cloud_ai / remote_mode_enabled / MSG_CLOUD_AI）殘留 = **0**。
- **狀態：S2 完成 ✅**（G1 達成：雲端開關與 DM 上傳完全移除、無殘留參照）。

## [S3] py_ai_train_run：移除 auto、新增 SYNC_MODE（D5/D10）

- `ai_inference_blocks.js`：BACKEND dropdown 移除 `AI_BACKEND_AUTO`（剩 local/remote）；其後新增 `SYNC_MODE` Dropdown（smart/always/skip）。
- `ai_inference_generators.js`：
  - 移除 `window.CocoyaUI.cloudAiEnabled` 全域讀取與 `auto` 分支 → `backendCode` 直接由欄位決定。
  - `train_model()` 注入簽名與呼叫處加入 `sync_mode`（local 路徑參數暫不使用；S4 於 remote 分支啟用）。
- i18n（`ai_inference/i18n/zh-hant.js`/`en.js`）：移除 `AI_BACKEND_AUTO`；新增 `AI_TRAIN_FIELD_SYNC_MODE`、`AI_SYNC_SMART/ALWAYS/SKIP`。
- **驗證**：node --check ×4 PASS；vite build PASS；cargo check PASS。
- **狀態：S3 完成 ✅**。

## [Handoff] 下一步（2026-08-28 收工點）

- 下一個 agent 第一動作：**S4** — `ai_inference_generators.js` 的 `train_model` remote 分支改為 host remote 呼叫（產出不含 SSH 內嵌；backend=remote 時拆「遠端訓練（.keras+labels）」與「本地 TFLite 轉換（依 MODEL_OUTPUT）」兩段，鐵律：TFLite 一律本地轉）。
- 注意：SYNC_MODE 語意僅 remote 生效；`sync_mode` 參數已進簽名，S4 直接使用。
- G2 Gate：S3+S4 完成後驗「積木 remote + sync 欄位」產出正確 host remote 兩段式程式碼。
- G3（SSH 精靈/smart 同步/遠端實測）維持 BLOCKED（主機不可連線）。
- 手動待測（無 SSH 也可測）：積木面板顯示「後端:本地/遠端」與「資料同步」下拉；local 訓練 round-trip 回歸。

## [S4+S5+S6] 遠端訓練執行鏈全接線（2026-08-28）

### 背景與使用者回報
- 使用者 SSH 主機已可連線（Winscp 驗證），但積木選遠端執行時仍在本地跑、SSH 精靈未出現。
- 靜態查證：前端攔截（base.js runCode 偵測 `backend='remote'`）與 SSH 精靈（dialogs.js `ensureSshConfig`，session 沿用）皆已存在且正則可匹配產出碼 `backend='remote'`；**真正的缺口是 host 端無 `startRemoteTraining` case**（VSIX switch 無此項、Tauri bridge 無此 case）→ 攔截訊息送出後被靜默丟棄；加上 webview 可能仍載舊 build。

### 本切片實作（三端）
| 檔案 | 異動 |
|---|---|
| `resources/dataset_manager/dataset_sidecar.py` | 新增 `trainRemote` 指令（移植 mvp_hand_gesture 04/05/06 已驗證流程）：SSH 連線 → 資料同步（smart=清單+size+mtime 差異上傳 / always=全量 / skip=跳過）→ `docker run --gpus all` 遠端訓練（即時 trainingLog 事件串流）→ SFTP 下載 `.keras`+`labels.txt` 至本地 outputDir。**TFLite 不在遠端轉（鐵律 D9：遠端 CPU 與 TFLite 不相容）**。遠端路徑 `~/cocoya_ai/sessions/{hostname}/dataset|models/{project}` 對齊 mvp |
| `src/handlers/trainingOps.ts` | 新增 `handleStartRemoteTraining`：從產出 code 解析 task_type/epochs/batch_size/learning_rate；datasetDir 相對路徑以 currentFilePath 目錄為基準；`sidecar.send('trainRemote', …)`；trainingLog 事件轉發前端；完成/失敗 postMessage `trainingComplete`/`trainingError` |
| `src/cocoyaManager.ts` | message switch 新增 `case 'startRemoteTraining'` |
| `ui/src/bridge/tauri.js` | 新增 `case 'startRemoteTraining'`：重用 `_handleDatasetCommand('trainRemote', …)`；因 Tauri 無 host handler，於 case 內解析 code 超參數、以 `get_project_anchor` 動態取專案根組絕對 localDatasetDir/outputDir |

### 施工插曲
- sidecar insert 時自帶 `elif command == "trainLocal":` 錨點造成重複行 → IndentationError → 已移除重複行。

### 測試命令與結果
- `py_compile dataset_sidecar.py`：PASS（exit=0）
- `node --check` tauri.js/base.js：PASS
- `npx tsc --noEmit`：PASS（exit=0）；`npm run compile`：PASS
- `npm run build --prefix ui`：PASS

### 手動測試狀態（待使用者執行）
1. **重啟/重載**（VSIX：重新載入視窗；Tauri：重啟 dev）——舊 build 是精靈沒出現的可能原因之一。
2. 積木後端=遠端 → 執行 → 應先彈 SSH 帳密框（session 沿用）→ 終端機出現 `[Remote] SSH 連線成功`、同步統計、Docker 訓練串流日誌。
3. smart 二跑：第二次應顯示「需上傳 0 檔」直接進訓練。
4. 遠端 Docker 映像 `cocoya-train-classifier` 需已存在（mvp 建置流程）。

### BLOCKED／風險
- Tauri 端 trainingLog 事件串流未驗證（`_handleDatasetCommand` 同步 request/response；`sidecar-event` 轉發訓練日誌待實測）。
- 遠端 detector 容器映像未驗證（僅 classifier 有 mvp 實績）。

### 下個 agent 第一動作
- 陪同使用者雙平台手動實測上述 1–4；通過後將 parity/manifest 補 `trainRemote`/`startRemoteTraining` 契約（S7），並處理 Tauri trainingLog 串流。
### [追加] Bug 修復：遠端訓練終端機「卡住」（密碼錯誤無任何輸出）

#### 使用者實測回報
- Tauri 積木選遠端執行，故意打錯密碼 → 終端機停在 `--- Remote training: sync=smart... ---`，無後續訊息。

#### 根因（兩個斷點）
1. `ui/src/bridge/tauri.js` sidecar-event 轉發：為 cameraStatus 設計的通用命名規則把 `trainingLog` 改名為 `datasetTrainingLog`，無人監聽 → 訓練日誌被吞。
2. 前端 UI（`ui/src/ui/base.js`）只監聽 `trainingComplete`，沒有 `trainingLog`/`trainingError` 的終端機輸出 → sidecar 其實有回 SSH 認證失敗 error，但畫面完全無顯示，看似卡住。
- sidecar 端（`dataset_sidecar.py` trainRemote）錯誤處理本身正確：auth 失敗會 rt_log + send_response error。

#### 修復
- tauri.js：sidecar-event 對 `trainingLog` 特判保留原名轉發 `{command:'trainingLog', message}`。

## [D 系列] 遠端訓練對齊本地輸出 + 模板自動同步（2026-08-28 追加）

### 需求
遠端目前執行 mvp train_classifier.py（--input_size/--lr、產 gesture_model.keras），與本地 classifier_train.py（依 model_output 產專案名 .keras/tflite + 報告）不一致。使用者要求配積木「模型輸出」欄位動作、輸出與本地一致，並自動同步模板。

### 關鍵決策（與使用者確認）
- D12 模板 smart 同步（只上傳變更）
- D13 bind mount templates（不改映像），每 run 全新 --rm
- D14 task_type 目錄結構（classifier 已有，detector/line_follower/table/feature/serial 預留）
- D15 遠端只產 keras+labels+history+curve；TFLite 與報告本地做
- 選項1：本地 int8 量化用「本地 dataset/<資料集名>」重建 representative dataset

### 實作
| 檔案 | 內容 |
|---|---|
| dataset_sidecar.py | ① 模板 smart 同步（find + zip + sftp + 遠端解壓到 ~/cocoya_ai/sessions/{machine}/templates）② docker_cmd 改掛 -v templates:/workspace + 依 task_type 選 python /workspace/{task}/{task}_train.py + 傳完整 hyperparams + --model_output(none→只產報告/keras→產keras) ③ 下載全部產物至 output_dir ④ 掃描產物絕對路徑(report/keras/curve/history) ⑤ model_output!=none 時呼叫本地 _local_convert_tflite.py 轉 TFLite ⑥ send_response 回傳產物路徑+tflitePaths |
| _local_convert_tflite.py | 新增：載入 keras，掃本地 dataset 建 representative，依 model_output 轉 int8/f32/all(.tflite / _f32.tflite)，對齊 classifier_train 命名；int8 無 representative 時降級 f32 |
| ui/src/bridge/tauri.js | startRemoteTraining 傳完整 hyperparams(validationSplit/dropout/augmentation/backbone/optimizer/dnnLayers/fineTune/modelOutput/taskType)；trainingComplete 帶 reportPath/kerasPath/curvePath/historyPath |
| src/handlers/trainingOps.ts | VSIX handleStartRemoteTraining 同步完整 hyperparams 解析與傳送 |

### 驗證
- py_compile dataset_sidecar.py / _local_convert_tflite.py PASS
- node --check tauri.js / base.js PASS；npm run build PASS
- tsc --noEmit / compile PASS

### 待手動實測（需 SSH 主機 + Docker 映像）
1. 遠端 docker run 掛 /workspace + python3 /workspace/classifier/classifier_train.py，產 {project}.keras + 報告
2. model_output=int8/f32/all → 本地轉出 {project}.tflite/_f32.tflite
3. 模板同步：改 train_templates 後重跑 → 偵測變更上傳；未改 → 零上傳
4. 開啟報告位置正確

### 下一步（下一個 agent）
D5 文件同步收尾（parity matrix/manifest/FILE_STRUCTURE/help），並實機驗證上述清單。注意：完整專案需遠端 Docker 映像含 cocoya common 依賴（tensorflow），且 classifier_train.py 的 common/cla 이 import 需在容器 python 路徑內（/workspace 掛載後 from common.* 需 /workspace 在 sys.path 或在容器 cwd=/workspace）。

- base.js：新增 `trainingLog` → appendTerminal(info)；`trainingError` → appendTerminal(err) + hideLoadingModal；`trainingComplete` 補終端機完成訊息。VSIX postMessage 路徑同時受益。

#### 驗證
- node --check ×2 PASS；npm.cmd run build --prefix ui PASS。
- 待使用者重啟 Tauri Dev 實測：錯誤密碼應顯示 `[Remote] 錯誤: SSH 連線失敗...`；正確密碼應看到 SSH 連線成功 → 同步統計 → Docker 訓練日誌串流。

### [追加] Bug 修復 2：遠端訓練「找不到本地資料集目錄」（Tauri）

#### 使用者實測回報
- 錯誤 IP/帳密測試，出現「找不到本地資料集目錄: dataset/classifier_dataset」——但專案（02_PC_train.xml）確實有資料集。

#### 根因
- `tauri.js startRemoteTraining` case 解析專案錨定時**直接 invoke 後讀 `anchor.projectRoot`，未走 `_normalizeAnchor()`**——AGENTS.md 2026-08-10 serde snake/camel 坑重演（其他 4 處皆有 normalize，唯此新案例漏掉）→ projectRoot 解析失敗 → 相對路徑未轉絕對 → sidecar 找不到目錄。

#### 修復
1. tauri.js：錨定解析改用 `_normalizeAnchor(await this.tauriInvoke('get_project_anchor'))`，並同步 `_anchor` 快照。
2. `dataset_sidecar.py trainRemote`：錯誤診斷順序調整——先 SSH 連線（連線失敗優先回報）再驗本地資料集目錄（原順序會讓連線問題被資料集錯誤掩蓋）。

#### 驗證
- py_compile PASS、node --check PASS、vite build PASS。
- 待重測：錯誤帳密 → `[Remote] 錯誤: SSH 連線失敗...`；正確帳密 → 連線成功 → 同步（projectRoot 正確解析 `examples/AI_01_classifier/dataset/classifier_dataset`）→ Docker 訓練。

### [追加] UX：遠端連線中動態提示
- 使用者回報：SSH timeout 等待期間終端機無動靜。
- 修復：sidecar 連線前 rt_log「連線中: user@host:port (timeout 15s)」；前端送出 startRemoteTraining 後啟動 0.7s 計時器持續補 '.'，收到第一筆 trainingLog/trainingError/trainingComplete 即清除。
- 驗證：py_compile / node --check / vite build PASS。

### [追加] UX 修正：點點計時器只顯示一個 "." 即停
- 根因：sidecar 連線前回報「[Remote] 連線中」本身就是第一筆 trainingLog → 計時器被立即清除。
- 修正：trainingLog 不再無條件清計時器，僅當訊息含「連線成功/同步檢查/錯誤/error」時才停；trainingError/trainingComplete 維持必停。
- 驗證：node --check、vite build PASS。

#### [追加] UX 修復：點點水平增加 + 連線失敗重彈精靈

##### 問題 1：點點每個都換行
- 根因：`terminal.js appendTerminal` 對同類型連續輸出會先插 \n（L91-92 合併規則），點點各自成行。
- 修復：`appendTerminal(text, type, inline)` 新增第三參數 inline 模式（同行水平附加、不換行），inline 判斷需在一般合併規則**之前**（首版順序錯誤已修正）；點點計時器改用 `appendTerminal('.', 'info', true)`。

##### 問題 2：連線失敗後再按執行不跳出精靈
- 根因：`dialogs.js ensureSshConfig` 只要 `window.CocoyaUI.sshConfig` 有值就沿用（session 沿用設計），連線失敗後帳密仍留在 session，跳過精靈。
- 修復：`base.js trainingError` 處理器中，若錯誤訊息符合連線類失敗（SSH/連線失敗/timed out/Authentication），清除 `window.CocoyaUI.sshConfig = null` → 下次執行 `ensureSshConfig` 重新彈出精靈讓使用者修正。

##### 驗證
- node --check terminal.js/base.js PASS；vite build PASS。

##### 待測
1. 連線中點點應水平向右增加（不再換行）。
2. 錯誤 IP/密碼 → timeout/認證錯誤 → 再按執行 → 精靈重新出現。

#### [追加] Bug 修復：SFTP 不展開 ~ 導致 [Errno 2] No such file

##### 現象
- 正確 SSH 帳密：連線中 .. 後出現 `[Remote] 錯誤: [Errno 2] No such file`。

##### 根因
- paramiko SFTP 子系統不展開 `~`：`sftp.put(tmp_zip, "~/cocoya_ai/...")` 直接回 SFTP NO_SUCH_FILE (errno 2)。
- 同一坑第二處：遠端解壓指令把 `~` 路徑放進 `python3 -c zipfile.extractall(...)`，zipfile 同樣不展開。
- 對照：下載段（L493-）已正確先取 realpath，上傳/解壓段漏了。

##### 修復（dataset_sidecar.py trainRemote）
- sftp.put 前先 `eval realpath` 取得 remote_dataset_real，remote_zip 改用絕對路徑。
- 解壓指令 ez/et 改用 remote_dataset_real。

##### 附帶確認
- 「連線成功後錯誤、再執行不重彈精靈」為正確行為（ensureSshConfig session 沿用；僅連線類錯誤才清 sshConfig）。

##### 驗證
- py_compile PASS。待使用者實測：同步統計 → 解壓 → Docker 訓練日誌。

### [追加] 遠端訓練 UX/鏈路修復（5 項）+ 中斷機制

#### 使用者回報
1. `openFolder` not handled in Tauri（訓練完成後開模型資料夾失敗）
2. 終端機沒有細節、部分文字沒換行、完成後點點繼續跑
3. 詢問中斷執行機制

#### 根因
- **關鍵**：sidecar `send_event` 把 `message` 放頂層（`{type,event,message}`），`tauri.js` 卻讀 `parsed.data.message` → 永遠空字串 → 所有 `[Remote]` 細節訊息被吞、關鍵字停表永不觸發（點點不停）。
- `base.js` `trainingComplete` 分支沒清 `_remoteConnTimer`。
- Rust `sidecar_send` 寫死 30s timeout：長訓練必爆（本次資料小 30s 內完成才沒踩到）。
- Tauri 無 `open_folder` 指令。
- 無中斷機制。

#### 修復
| 檔案 | 內容 |
|---|---|
| `ui/src/bridge/tauri.js` | trainingLog 讀 `parsed.message`（頂層）；trainRemote 傳 `timeoutSecs:3600`；`_handleDatasetCommand` 支援 opts.timeoutSecs；新增 `stopRemoteTraining`/`openFolder` case；`stopCode` 順帶送 `stopTraining` |
| `ui/src/ui/base.js` | `trainingComplete` 分支清點點計時器 |
| `resources/dataset_manager/dataset_sidecar.py` | docker run 加 `--name cocoya_train_<project>`；記錄 `_remote_train` 工作階段；新增 `stopTraining`（另開 SSH `docker rm -f`）；完成/錯誤時清工作階段 |
| `src-tauri/src/commands/dataset.rs` | `sidecar_send` 加 `timeout_secs: Option<u64>`（預設 30） |
| `src-tauri/src/commands/app.rs` + `lib.rs` + `permissions/commands.toml` | 新增 `open_folder`（explorer/open/xdg-open）二階段權限定義 |
| `src/handlers/envOps.ts` | `handleStopCode` 順帶 sidecar `stopTraining` |

#### 驗證
py_compile PASS、node --check ×2 PASS、cargo check PASS、tsc --noEmit PASS、vite build PASS。

#### 待手動驗證（需重啟 Tauri Dev）
- 終端機應出現完整 [Remote] 細節（連線成功/同步統計/Docker 日誌）
- 點點在完成後停止
- 停止按鈕可中斷遠端訓練（docker rm -f）
- 訓練完成後開啟模型資料夾

### [追加] 遠端訓練進度條編碼修復

#### 根因
- paramiko `exec_command` 的 stdout 迭代給 bytes，未 decode → Keras 進度條 Unicode 方塊字元（`█`/`━`）變亂碼。
- Keras verbose=1 用 `\r` 原地更新進度；本地訓練走 subprocess `universal_newlines=True`（`\r`→`\n`），遠端未轉換 → 進度條碎片黏成一團。

#### 修復
`dataset_sidecar.py` 遠端 Docker 輸出讀取迴圈：每行 `decode(utf-8, replace)` + `\r`→`\n` 切分後逐行 rt_log —— 與本地訓練輸出格式一致（每個進度更新獨立一行、中文正常）。

#### 驗證
py_compile PASS。待遠端實測確認輸出顯示。

### [追加] 遠端訓練輸出格式精修（ANSI + epoch 摘要）

#### 使用者實測回報
進度條帶 ANSI 色碼（[1m/[32m），且每個 step 一行太冗長。

#### 修復（dataset_sidecar.py 遠端 Docker 輸出迴圈）
- regex 剝除 ANSI escape 色碼（\x1b[...m）。
- Keras 進度條每 epoch 只輸出：`Epoch N/M` 標題 + 最後一步完整摘要行（中間 step 更新略過）。

#### 預期輸出
```
Epoch 5/5
11/11 [==============================] - 3s 244ms/step - accuracy: 0.9068 - loss: 0.2730 - val_accuracy: 0.9125 - val_loss: 0.3723
```

#### 驗證
py_compile PASS。待遠端實測。

### [附加修復] dataset_sidecar.py 殘留舊 docker_cmd（input_size 未定義）
- 症狀：Pylance 報 input_size 未定義（L593）。
- 根因：D2 改版時，新 docker_cmd（563-584）與舊 docker_cmd（585-595，含 --input_size/--lr）並存；舊版覆蓋新版且引用已刪的 input_size。
- 修復：刪除 585-595 舊版殘留，保留唯一新 docker_cmd。
- 驗證：Select-String 僅 1 處 docker_cmd、無 input_size 引用；py_compile PASS。

### [附加修復] docker ENTRYPOINT 覆蓋問題
- 症狀：遠端報 `can\'t open file train_classifier.py (exit 2)`。
- 根因：mvp Dockerfile.train 設 `ENTRYPOINT ["python", "train_classifier.py"]`。docker run 後綴 `python3 /workspace/classifier/classifier_train.py` 被當 ENTRYPOINT 參數，沒被執行；ENTRYPOINT 自身 python train_classifier.py 因 /workspace 掛載覆蓋後無此檔而失敗。
- 修復：docker_cmd 加 `--entrypoint python3`，後綴改 `/workspace/classifier/classifier_train.py`（絕對 path）。
- 驗證：py_compile PASS。

### [追加] 修復：遠端 templates/ 空卻誤報「已是最新」

#### 根因
sidecar `local_templates_root` 路徑解析錯誤——`dirname(__file__)/../..` 是專案根，原 code join `train_templates` 少了 `resources` 層 → `os.walk` 掃不存在路徑回空 dict → `tmpl_changed=[]` → 誤判「模板已是最新」，從未上傳。遠端 `templates/` 因此是空的，Docker 找不到 `/workspace/classifier/classifier_train.py`（exit 2）。

#### 修復（dataset_sidecar.py）
- `local_templates_root` 改為 `../..` + `resources/train_templates`（已驗證解析 = `C:/Workspace/cocoya/resources/train_templates`，26 檔）。
- 防誤判護欄：`tmpl_local` 為空 → raise RuntimeError（含本地路徑與遠端檔數），絕不再靜默跳過上傳。
- 新增診斷行：`[Remote] 模板同步檢查: 本地 N 檔，遠端 M 檔，需上傳 K 檔`。

#### 驗證
py_compile PASS；路徑解析實測 26 檔存在。

#### 請重測
重啟 Tauri Dev（sidecar 重啟載新 code）→ 遠端訓練：第一次應顯示「需上傳 26 檔」→ 解壓 → Docker 找得到 classifier_train.py。

### [追加] 訓練/驗證集切分改為分層抽樣（stratified split）

#### 檢查結論
- classifier（`common/classifier_dataset.py`）：原用 `image_dataset_from_directory(validation_split=...)` 為**全域隨機切**，非分層抽樣——類別不平衡時驗證集可能某類過少甚至 0 張。
- detector（`common/detector_dataset.py`）：bbox 回歸單類別，shuffle+take 隨機切即等效分層，維持現狀。

#### 修改（classifier_dataset.py load_image_dataset）
- 每個分類各自 `random.Random(seed).shuffle` 後按 validation_split 切出驗證集（保證各類比例一致）。
- 教學保護：類別 ≥2 張時驗證集至少 1 張、訓練集至少 1 張；切後任一集合為空則明確報錯。
- 以 `tf.data.Dataset.from_tensor_slices` + `decode_image/resize/one_hot` 建資料集，train shuffle / val 不 shuffle。
- 輸出分層報告：`分層抽樣 (stratified split):` + 每類 `train X / val Y`。

#### 驗證
py_compile PASS。本地/遠端訓練共用此模板（遠端 bind mount templates），SSH 恢復後一併實測：本地訓練應印出分層報告且 val 樣本數各類均衡。

#### 待辦
- SSH 連線再次中斷，遠端 templates 上傳修復與分層抽樣遠端實測均標記待測。

### [追加] 物件偵測資料集改為分層抽樣

#### 使用者指正
先前判定「detector 為 bbox 單類別回歸、隨機切即可」是錯的——detector_dataset.py 實際讀取 YOLO class_id 並支援多類別（labels.txt + 各類統計），多類別不平衡時 shuffle+take 會讓小類別驗證集失衡。

#### 修復（common/detector_dataset.py）
- 切分改為依 class_id 分層：各類 seeded shuffle 後按 validation_split 切驗證集，各類比例一致。
- 教學保護：類別 >= 2 張時驗證/訓練各至少 1 張；單一樣本類別只進訓練集；切後任一集合為空明確報錯。
- 輸出分層報告（與 classifier 相同格式）：`分層抽樣 (stratified split): label: train N / val M`。
- 管線重構：make_ds(paths, boxes, shuffle)，train 每 epoch reshuffle、val 不 shuffle，下游 detector_train.py（prepare_detector_dataset 擴增、class_counts）介面不變。

#### 驗證
py_compile PASS；detector_train.py 呼叫介面比對一致。

#### 待測
SSH 恢復後：遠端 templates 上傳修復 + classifier/detector 分層抽樣報告實測。

### [追加] AGENTS.md 新增「訓練集切分鐵律：分層抽樣」

- 依使用者指示，將分層抽樣規範固化為 AGENTS.md 鐵則（插入於「多視窗完整性規範」與「前端狀態訊息慣例」之間）：
  - 所有帶類別標籤的訓練模板必須分層抽樣，禁止全域隨機切分。
  - 已實作：classifier_dataset.py（依資料夾類別）、detector_dataset.py（依 YOLO class_id），含教學保護與分層報告。
  - 未來模板注意：line_following 比照 detector 依標註類別分層；table/feature/serial 依 label 欄位分層（回歸型才允許隨機切並註明）；新模板重用既有分層函式。
- 背景查證：train_templates 目前僅 classifier/ 有實際腳本（sidecar 已預留 line_following/line_following_train.py、table/table_train.py、feature/feature_train.py、serial/serial_train.py 路徑對應，模板尚未實作），規範提前寫入以免未來開發重蹈全域隨機切。
