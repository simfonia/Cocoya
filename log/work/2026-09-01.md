# 2026-09-01 工作日誌

## [Dataset Manager Refactor] Stage 5 切片 6：i18n key parity（#task[Dataset Manager Refactor - Stage 5-6]）

### 施工範圍宣告

```text
Stage：Stage 5 切片 6（§8.2 步驟 4：比對 i18n key，刪未使用、補缺漏）
Owner：Cline (act mode)
允許修改：ui/src/modules/dataset_manager/i18n/{zh-hant,en}.js、ui_layout.js / importUseCases.js（僅 key 字串修正）、parity matrix、todo、log/work/*
禁止修改：公開 API、spec.js / ui_canvas.js / ui_components.js、core/* / io/* 既有行為、ESLint config、Vite classic warnings；不刪 ui_layout.js
前置 Gate：HEAD = 69105aa（Stage 5-5），工作樹 clean
本階段成功條件：zh/en key 集合一致、無缺漏 key、未使用 key 清除、自動化全綠
預計驗證命令：node --check ×4、node --test、npm run build --prefix ui
需要人工測試的平台：VSIX + Tauri Dev（T5-1/T5-2 語系，登錄 backlog）
```

### 盤點方法

- 工具：`temp_scripts/i18n_parity_scan.cjs`（新增）——抽兩語系 key 集合比對 + 掃描 `ui/src`（排除 i18n/backup）中 `t('KEY', ...)` 呼叫（i18n.js 的 `t()` 會自動加 `DSM_` 前綴，故掃描須以「無前綴名」比對；首版掃描器以 `DSM_` 前綴搜尋導致誤判全 unused，已修正）。
- 盤點結果（修正前）：zh/en 定義集合本就 139/139 一致（無缺漏），但發現 7 個「程式呼叫 vs 定義」不一致。

### 發現與修正（切片 6a：key 呼叫錯誤）

| 位置 | 問題 | 修正 |
|---|---|---|
| `ui_layout.js:1065` | `t('STATUS_CAPTURETTING')` 拼字錯誤，i18n 定義為 `DSM_STATUS_CAPTURETING` | 改 `t('STATUS_CAPTURETING')` |
| `ui_layout.js:1495/1501` | `t('DSM_NEED_ANCHOR')` 雙重前綴（實際查 `DSM_DSM_NEED_ANCHOR`，永遠 fallback） | 改 `t('NEED_ANCHOR')` |
| `importUseCases.js:134/138/141/155` | `t('DSM_IMPORT_*')` 雙重前綴 ×4 | 去 `DSM_` 前綴 |

影響：上述訊息在 English 語系下永遠顯示 fallback 中文；修正後英文翻譯首次真正生效。

### 未使用 key 清除（切片 6b）

- 兩語系各刪 21 key：`UPLOAD_ZIP`、`ANNOTATION_INFO/FILE/NAV_PREV/NAV_NEXT/NEW_CLASS_CONFIRM/NEW_CLASS_CANCEL/UNCLASSIFIED/UNKNOWN/UNSAVED_WARNING`、`ERROR_READ_FAILED`、`ERROR_UPLOAD_RESULT_IGNORED`（遠端面板移除後無使用處，依 08-31 handoff 檢查項）、`CLASSIFY_ADD_LABEL/CLASSIFY_NEW_LABEL_PLACEHOLDER`、`SAVE_PROGRESS/SAVE_PROGRESS_TOOLTIP/STATUS_SAVING_PROGRESS/SUCCESS_SAVE_PROGRESS/ERROR_SAVE_PROGRESS_FAILED/ERROR_SAVE_PROGRESS_NO_ANCHOR`（「儲存進度」按鈕已於 autosave 改革移除）、`ERROR_LOAD_PROGRESS_FAILED`。
- **保留** `CLOSE_UNSAVED_CONFIRM`：`ui_layout.js:1523` 註解「保留 CLOSE_UNSAVED_CONFIRM i18n key 供未來情境使用」為既有明確決策。
- 逐 key 以 Select-String 對 `ui/src`（排除 i18n/backup）二次驗證 UNUSED 才刪除。

### 驗證

- `temp_scripts/i18n_parity_scan.cjs`：zh/en 各 118 key、集合完全一致、missing=0、unused 僅剩刻意保留的 CLOSE_UNSAVED_CONFIRM
- `node --check`：zh-hant.js / en.js / ui_layout.js / importUseCases.js 全 PASS
- `node --test`（11 個 *.test.mjs）：80 pass / 0 fail
- `npm.cmd run build --prefix ui`：PASS（532ms）

### 手動測試狀態

| 案例 | 平台 | 狀態 |
|---|---|---|
| T5-1 語系 key parity（en 下操作全流程，確認無中文殘留/undefined） | VSIX + Tauri Dev | 自動化 parity PASS；實機待測（backlog） |
| T5-2 locale refresh race | VSIX + Tauri Dev | NOT_STARTED（下一切片） |

### FAIL / BLOCKED

- 沿既有：Tauri Release 安裝版 smoke、SSH/SFTP 驗證 BLOCKED（未標 PASS）。
- 無新增 FAIL/BLOCKED。

## 下次啟動方向 (Next Steps)

- Stage 5 收尾：§8.2 步驟 5-6（locale 初始化/refresh/reload 測試、文案長度與按鈕溢位檢查）+ T5-1~T5-3 實機集中測 → Stage 5 Gate 簽核。
- 之後進 Stage 6（文件同步、冗餘清理、VSIX/Tauri 相容性確認）。
- 待收斂債：`--dsm-*` dark 值雙來源、dev 模式 sidecar 路徑優先序。

## AI Agent Handoff（Stage 5 切片 6：i18n key parity）

- 日期：2026-09-01
- Agent：Cline
- 階段：Stage 5 切片 6（§8.2 步驟 4）
- 狀態：READY_FOR_TEST（自動化全綠；T5-1/T5-2 實機待測）
- HEAD：69105aa（工作樹原 clean；本切片修改見下）
- 本次目的：i18n key parity（刪未使用、補缺漏、修 key 呼叫錯誤）
- 已完成：6a key 呼叫修正 6 處（1 拼字 + 5 雙重前綴）、6b 兩語系各刪 21 未使用 key、parity 118/118
- 修改檔案：`ui_layout.js`（1+2 處 key 字串）、`application/importUseCases.js`（4 處 key 字串）、`i18n/zh-hant.js`、`i18n/en.js`（各 -21 key）、`temp_scripts/i18n_parity_scan.cjs`（新，盤點工具）、`log/mappings/DatasetManagerParityMatrix.md`、`log/todo.md`、本檔
- 未完成或風險：CLOSE_UNSAVED_CONFIRM 為刻意保留（勿再刪）；T5-1/T5-2 實機未測
- 測試命令與結果：node --check ×4 PASS；node --test 80/80 PASS；vite build PASS（532ms）；parity scan 118/118 missing=0
- 手動案例與結果：T5-1/T5-2 待實機（backlog）
- 證據位置：本檔、parity matrix Verification log
- 文件同步狀態：parity matrix、todo.md、本檔已同步；FILE_STRUCTURE 無結構異動（temp_scripts/ 既有目錄）
- 下一個 agent 第一動作：Stage 5 收尾切片——§8.2 步驟 5（locale refresh/reload：refreshI18n 重建鏈路審查）與步驟 6（文案長度/溢位檢查），再集中執行 T5-1~T5-3 實機
- 禁止重做或修改的事項：不得刪 CLOSE_UNSAVED_CONFIRM；不得動 ESLint config/Vite classic warnings；不得將 BLOCKED 標 PASS
- 需要產品決策的問題：無
- 交接者：Cline

## Stage 5 收尾切片：locale refresh race 修復（§8.2 步驟 5）

### 審查範圍

- `index.js`：`getPreferredLocale` / `getLocaleCandidates` / `loadI18n` / `refreshLocale` 鏈路
- `ui_layout.js:1467` `refreshI18n()`：modal 重建 + 應重開語意
- `lifecycle.js:162`：語系切換 → `window.CocoyaDataset.refreshI18n()`

### 發現與修正

- **T5-2 race（`index.js` loadI18n）**：舊實作以 `document.querySelector(script[src=...])` 判斷已載入；同一 URL 的 script 仍在載入中時第二次呼叫會**立即 resolve**，`refreshI18n()` 在新語系鍵值就緒前重建 UI → 顯示舊語系。
- 修正：改為 `localeScriptPromises` Map 記錄每個 URL 的 in-flight promise（onload 才 resolve、onerror 清除記錄並 fallback 下一候選），行為契約不變（最終 resolve、回退鏈 zh/en 不變）。
- `refreshI18n()` 本體審查：modal 重建走 `initDatasetManagerUI()`，Bridge listener 訂閱洩漏已於 Stage 4 切片 2 修復（offBridgeMessage 掛 modal 並於卸載前解除）；`state.isOpen`/重開語意正確，無需修改。

### 驗證

- `node --check` index.js：PASS
- `npm.cmd run build --prefix ui`：PASS（296ms）

### 手動測試狀態

| 案例 | 平台 | 狀態 |
|---|---|---|
| T5-1 語系 key parity（en 全流程目視） | 實機 | **PASS**（使用者 2026-09-01 確認） |
| T5-2 Locale refresh race（開啟 DM 中切語系、快速開關 modal、重進 annotation/classification） | VSIX + Tauri Dev | 自動化（in-flight promise 修復）+ 實機待測 |
| T5-3 主題矩陣 / §8.2 步驟 6 文案長度與溢位目視 | VSIX + Tauri Dev | NOT_STARTED（實機集中測） |

## AI Agent Handoff（Stage 5 收尾：locale refresh race 修復）

- 日期：2026-09-01
- Agent：Cline
- 階段：Stage 5 收尾（§8.2 步驟 5）
- 狀態：READY_FOR_TEST（T5-1 已 PASS；T5-2/T5-3 實機待測）
- HEAD：3956591（切片 6 已 commit）+ 本切片 index.js 修改
- 修改檔案：`ui/src/modules/dataset_manager/index.js`（loadI18n race 修復）、log 三件
- 測試命令與結果：node --check PASS、vite build PASS（296ms）
- 下一個 agent 第一動作：實機 T5-2/T5-3 → Stage 5 Gate 簽核（§8.4 六項核取）→ Stage 6（§9：文件同步、冗餘清理、VSIX/Tauri 相容性確認）
- 禁止重做或修改的事項：不得刪 CLOSE_UNSAVED_CONFIRM；loadI18n 的 in-flight promise 防護不得移除；BLOCKED 不得標 PASS
- 需要產品決策的問題：無
- 交接者：Cline

## Stage 6 切片 1：D6-3 靜態清理掃描 + 死 wrapper 清除 + D6-1/D6-2 文件核對

### D6-3 掃描證據（§9.2 D6-3 指令精神，PowerShell Select-String 對照）

- **direct `window.CocoyaBridge`**：dataset_manager 內僅 `io/bridge.js`（唯一 Bridge Port adapter，合法 owner：io 層）；其餘檔案 = 0。
- **全域 `.emit(`**（dataset_manager 前端 + src-tauri/src）：0 處。
- **legacy DOM id**（`dataset-dir-input`、`dataset-import-status`）：0 處。
- **未使用 export**：0（`temp_scripts/unused_export_scan.cjs`，26 檔）。
- **死 wrapper 清理**：`ui_layout.js` 內 5 個「宣告但零呼叫、未 export」的委派 wrapper 刪除——`renderColumnRow`、`bindCanvasKeyboardEvents`、`deleteSelectedAnnotation`、`updateClassifyProgress`、`bindClassificationKeyboardEvents`（真正實作在 ui/panels.js、ui/annotation.js、ui/classification.js，外部引用皆指向 presenter 本體）＋清掉 3 個孤兒註解。
- **重複 DOM id 結論（合法保留）**：`annotation.js` 與 `classification.js` 共用 7 個面板 id（`dataset-annotation-back`/`annotation-thumbnails` 等）——兩模式互斥、同時僅一份存在於 DOM、各自有 `existingBackBtn` 防重複 guard；`dataset-column-list`/`dataset-json-preview` 為 modal.js 模板定義 + ui_layout 引用。皆記錄保留原因。
- **同名函式**（ui_layout vs application/ui）：Stage 4 設計的委派 wrapper（呼叫點零改動），非重複 handler。
- **施工事故**：中間以 temp_scripts/fix_worklog_order.cjs 重排區段時腳本錯誤導致 Stage 5 收尾區段遺失，已自 git HEAD（7cd6c2e）完整還原後再追加本區段；歷史紀錄零損失（已比對 section 標題清單）。

### D6-1 / D6-2 文件核對結果

- `FILE_STRUCTURE.md`：i18n 描述移除已刪的 SAVE_PROGRESS_* 系列、補 118 key parity；index.js 補 locale race 防護註記；ui_layout.js 改述為協調層＋委派 wrapper。
- `log/mappings/DatasetManager.html`：公開 API 表補齊 `toggle/createSpec/detectSchema/validateSpec/refreshI18n/init/version/DatasetSpec/constants`（對照 index.js 實際掛載，無 breaking change）；§3 遠端/雲端指令標記「前端已移除、後端保留」（切片 5c）；§4 Importer 更正為 `application/importUseCases.js` 並補 canonical 匯入閘。
- `docs/backend_api_manifest.md`：Dataset command 契約未變（本次零後端修改），暫不動。

### 驗證

- node --test 全套（core/io/application/ui）：80 pass / 0 fail
- npm.cmd run build --prefix ui：PASS
- node --check ui_layout.js：PASS

## AI Agent Handoff（Stage 6 切片 1：靜態清理 + 文件核對）

- 日期：2026-09-01
- Agent：Cline
- 階段：Stage 6 切片 1（D6-3 掃描 + 死 code 清理 + D6-1/D6-2 部分核對）
- 狀態：READY_FOR_TEST
- HEAD：7cd6c2e + 本切片
- 修改檔案：`ui_layout.js`（刪 5 死 wrapper + 孤兒註解）、`log/mappings/DatasetManager.html`（API 表補齊/遠端節狀態/Importer 位置）、`FILE_STRUCTURE.md`（i18n/index/ui_layout 描述）、log 文件
- 測試命令與結果：node --check PASS；node --test 80/80；vite build PASS
- 下一個 agent 第一動作：Stage 6 續行——D6-1 深度核對（backend_api_manifest.md 各 Dataset command vs VSIX handlers/Tauri commands 逐項；DatasetManager_DevGuide.html 建立或標記 NOT_STARTED）；AGENTS.md 訊息責任定義（§8.1「定義 UI、VSIX Host、Rust、sidecar 訊息責任」尚未落成文件）
- 禁止重做或修改的事項：同名委派 wrapper（ui_layout → presenter）為 Stage 4 設計契約，除已刪的死 wrapper 外不得再刪；annotation/classification 共用面板 id 為互斥模式合法設計；BLOCKED 不得標 PASS
- 需要產品決策的問題：無
- 交接者：Cline

## Stage 6 切片 2：backend manifest 逐項核對 + 訊息責任定義 + DevGuide 建立

### D6-1 深度核對（backend_api_manifest.md vs 兩端實作）

- Rust 端逐一確認（`lib.rs` invoke_handler 註冊 + 簽名）：`get_project_anchor`(file.rs:40)、`dataset_save_progress`(575)、`dataset_load_progress`(623)、`dataset_import_from_folder`(487)、`dataset_upload_chunk`(dataset.rs:417)、`pick_folder`/`delete_file`/`export_dataset`（lib.rs 註冊齊全）——參數命名與 manifest 完全一致。
- VSIX 端 `datasetOps.ts`：`handleDatasetImportFromFolder/DatasetExport/DatasetUploadArchive/DatasetSaveProgress/DatasetLoadProgress` 齊備，result command 命名一致。
- **修正**：`get_project_anchor` 在 manifest 重複兩列 → 合併為一列並補 serde camelCase 鐵律註記（`isAnchored`/`projectRoot`）。
- 結論：**Dataset command 契約兩端與 manifest 一致，無 breaking change（D6-2 續核通過）**。

### 訊息責任定義落成（§8.1 未竟項）

- 新增 AGENTS.md「Dataset Manager 訊息責任定義」：UI（io/bridge.js 唯一通道 + i18n 文案責任）/ VSIX Host（結構化 errorCode，不做 i18n）/ Tauri（camelCase + emit_to + 路徑權威）/ Sidecar（stdout JSON，不直接對話 UI）；鐵律：錯誤碼在後端、人類文案在前端 i18n。

### DatasetManager_DevGuide.html 建立

- 依「文件責任矩陣」責任（開發 SOP）建立：新增資料集類型、i18n key（雙重前綴禁令）、--dsm-* token 三處同步、bridge adapter/錯誤碼/測試 SOP。

### 驗證

- node --check PASS；vite build PASS（文件切片，無程式碼行為變更）。

## AI Agent Handoff（Stage 6 切片 2）

- 日期：2026-09-01
- Agent：Cline
- 階段：Stage 6 切片 2（D6-1 backend manifest 核對 + 訊息責任定義 + DevGuide）
- 狀態：READY_FOR_TEST
- HEAD：e437423 + 本切片
- 修改檔案：`docs/backend_api_manifest.md`（get_project_anchor 去重複 + camelCase 鐵律註記）、`AGENTS.md`（訊息責任定義）、`log/mappings/DatasetManager_DevGuide.html`（新）、log 文件
- 測試命令與結果：node --check PASS；vite build PASS（本切片零程式碼行為變更）
- 手動案例與結果：D6-2 公開 API 相容性（§3.3 B0-1 清單）需實機 console 執行 `Object.keys(window.CocoyaDataset).sort()` 對照——登錄 backlog
- 下一個 agent 第一動作：Stage 6 收尾切片——①`log/plan/DatasetManagerOptimization.md`/`DatasetManagerProgressAndGuardrails.md`/`DATASET_MANAGER_PLAN.md` 標記 superseded/已實作；②docs/system_spec.html Dataset Manager 章節與現行三層架構核對；③Stage 6 Gate（§9.3 五項）簽核 → Stage 7 總驗證
- 禁止重做或修改的事項：公開 API 零 breaking change 已確認，勿改 window.CocoyaDataset 簽名；BLOCKED 不得標 PASS
- 需要產品決策的問題：①DatasetManager_DevGuide.html 內容深度是否足夠或需擴充（新增類型 SOP 細節）②transitional spec.js t() 耦合是否本次收斂為 error code（建議列 Stage 7 後債）
- 交接者：Cline

## Tauri DM 深色確認框白底修復（2026-09-01，Stage  ̄ 6 追加）

- **根因**：tauri.js `_handleNativeDialogs` 的 alert/confirm 原本走 `@tauri-apps/plugin-dialog` 的 OS 原生 `message()/ask()`——原生視窗白底，`--dsm-*` token 管不到，故深色下 DM「清除資料」確認框白底。
- **修正**：`alert`/`confirm` 改走既有 token 化自訂對話框（`_showAlertDialog`/`_showConfirmDialog`，與 prompt 同套 `.cocoya-prompt-*` 樣式）；樣式抽出共用 `_ensureDialogStyles()`（幂等）；Enter/Escape/背景點擊關閉、`okBtn.focus()`、listener 於 settle 時解除，無殘留回呼。confirm 回傳 boolean 語意與原生 ask() 一致。
- **保留**：`_handleExamplesSaveDialog`（覆蓋內建範例確認）仍用原生 plugin-dialog ask——非本次回報、涉及額外 label 行為，記入 todo 債。
- **既有伴隨問題（沿用，記 todo）**：深色下 `.cocoya-prompt-btn:hover` 的 `--dsm-btn-hover-bg` fallback 白，深色 hover 仍白底（prompt/confirm/alert 皆然）；與原 prompt 行為一致，非本次引入，留作後續 token 收斂。
- **驗證**：node --check tauri.js PASS；vite build PASS（166ms）。
- **待實機**：Tauri 深色下 DM 清除資料確認框應為深底、三主題（light/dark/candy）換膚生效。

## Tauri 覆蓋範例確認框一併 token 化（2026-09-01 追加）

- `_handleExamplesSaveDialog`（覆蓋內建範例）改走 `_showConfirmDialog`：okLabel 優先 `MSG_SAVE`/`BKY_SAVE`、cancelLabel `MSG_CANCEL`、訊息優先 `BKY_EXAMPLES_OVERWRITE_CONFIRM`（缺 key 落回原中文）；Enter/Escape/背景語意與原生 ask 一致。
- `_handleCloseDialog` 的 fallback 分支（非 showSaveConfirm 環境）一併改走 `_showConfirmDialog`（「確定」即關閉）。
- **結果**：tauri.js 內 `@tauri-apps/plugin-dialog` 殘留 = 0；Tauri 所有 confirm/alert 對話框統一token 化、深色/candy 換膚生效。
- **驗證**：node --check PASS；vite build PASS（155ms）。待實機：深色/candy 下範例儲存（EXAMPLES_PATH 情境）與視窗關閉 fallback。

## Tauri/VSIX 換主題 dirty 專案被無預警重載修復（2026-09-01）

- **根因**：主題切換 `setMode`（theme_manager.js）直接 `reloadWebview`/`location.reload()`，reload 銷毀 JS 記憶體中 dirty 的未存修改；`initializeCocoya`（lifecycle.js:181）空 workspace 就 `createDefaultBlocks()`（變全新未命名）＋`setDirty(false)`。Ctrl+R/F5 早已攔截（lifecycle 168-179 註解承認此坑），唯主題切換漏網。
- **修正（使用者選「兩者都要」）**：
  1. `theme_manager.setMode` 改 async：dirty 時彈三選（保留並換/捨棄並換/取消）；keep→寫快照、discard/cancel→清快照不保留、cancel→不 reload 不改偏好；乾淨→清殘留快照後直接換。
  2. `persistence.js` 新增 `snapshotWorkspaceForReload/clearReloadSnapshot/consumeReloadSnapshot`（sessionStorage，reload/重建同 origin 保留）。
  3. `lifecycle.initializeCocoya` 建好 workspace 後若有快照則還原 xml+檔名+唯讀、維持 dirty（抑制原本 `setDirty(false)`）。
- **三選對話框**：theme_manager 自足 `_showReloadChoice`（token 化 + body.vscode-dark 覆寫，跨平台一致，Escape=取消）。setMode 兩呼叫端（persistence.js:400/base.js:411）皆 fire-and-forget，async 相容。
- **遺留**：discard 後仍會「前端未命名、後端仍錨定」（lifecycle 已知避坑），為使用者明示捨棄之合理結果。
- **驗證**：node --check ×3 PASS；vite build PASS（260ms）。待實機：dirty 下切主題三選各分支 + clean 直接換 + 深色三選框。

## Minimap 換主題後空白修復（2026-09-01 追加）

- **根因**：`_restoreReloadSnapshot` 在 `Blockly.Events.disable()` 下 `clear()+domToWorkspace(xml)` 還原快照；minimap 依賴主工作區的事件 mirror 同步（workspace.js initMinimap 覆寫 mirror），還原期間事件被 disable、不發 mirror 事件 → minimap 維持空白。
- **修正**（lifecycle.js `_restoreReloadSnapshot`）：快照還原完成後強制呼叫 `window.CocoyaApp.refreshMinimap()`（workspace.js:139 直接 reconstruct minimap workspace + zoomToFit + svgResize，不依賴 mirror/事件）。
- **驗證**：node --check ×2 PASS；vite build PASS（172ms）。待實機：換主題（dirty keep/clean）後 minimap 應顯示還原內容。

## Stage 6 收尾切片：歷史文件標記 + system_spec 補 DM 章節（§9.1）

- 3 份歷史計畫標記 SUPERSEDED（保留原文，僅加檔頭狀態標記）：`DATASET_MANAGER_PLAN.md`、`log/plan/DatasetManagerOptimization.md`、`log/plan/DatasetManagerProgressAndGuardrails.md`。
- `docs/system_spec.html` 原本**無** Dataset Manager 章節；增補〈24. Dataset Manager 模組規範〉：core/io/ui 三層架構、canonical 儲存、雙平台原則（含 camelCase/錯誤碼）、安全不變量，並指向 backend_api_manifest / DatasetManager.html / DevGuide / ParityMatrix。
- **Stage 6 Gate（§9.3）盤點**：①文件責任矩陣已落實 ✅ ②API/command/event/permission/resource 文件一致 ✅（切片2 逐項核對）③dead code/重複 handler 已清理或標記 ✅（切片1）④公開 API 相容性：靜態核對通過，D6-2/B0-1 實機 console 對照仍在 backlog ⚠️ ⑤FILE_STRUCTURE 只描述現行結構 ✅；⑥審核者簽核 ❌ 待使用者。
- **驗證**：HTML 結構目視正確（section 標籤平衡、footer 保留）。

## Tauri Release 模式路徑錯誤修正（PermissionError: model/）（2026-09-01）

- **根因**：訓練模板使用相對路徑 `model/classifier_dataset` 作為 output_dir。在 VSIX 與 Tauri Dev 模式，sidecar CWD 在專案根目錄，路徑正確；但在 Tauri Release 模式，sidecar 位於 `C:Program FilesCocoyaesourcesdataset_manager`，相對路徑解析到 Program Files，標準使用者無權寫入 → PermissionError [Errno 13]。
- **修正**：
  1. `src-tauri/src/commands/dataset.rs` `start_sidecar`：從 `state.current_paths` 取專案根目錄，以 `COCOYA_PROJECT_ROOT` 環境變數傳入；同時補 `PYTHONIOENCODING=utf-8`/`PYTHONUNBUFFERED=1`。
  2. `src-tauri/src/commands/python.rs` `start_training`：同上（此函式也會 spawn sidecar）。
  3. `resources/dataset_manager/dataset_sidecar.py` `if __name__=="__main__"`：啟動時 `os.chdir(os.environ["COCOYA_PROJECT_ROOT"])` 到專案根目錄，確保所有相對路徑（訓練輸出、匯出等）正確解析。
- **施工事故**：首次編輯误將 `.env()` 放在 `.spawn()` 之後（Result 沒有 env 方法），已修正为放在 `.spawn()` 之前。
- **驗證**：`cargo check` PASS（僅 2 個既有 warning）。待實機：在正體中文 Windows 跑 `cargo tauri dev` + `02_PC_train.xml` 確認 model/ 正確寫入專案根目錄下。
