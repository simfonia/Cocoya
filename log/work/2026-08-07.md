# 2026-08-07 工作日誌

## 工作內容

### BBox 標註 UI 除錯（Tauri 版）

#### 需求
測試及除錯 Tauri 版 bbox 標註 UI（承接 2026-08-06 的全寬 3 欄布局重構），修正 5 個問題：
1. 上方副標題「Dataset Spec」不對 → 改為動態顯示「物件偵測標註」(i18n)
2. 左欄縮圖無法捲動
3. 一進入標註模式每張縮圖都出現綠勾，切換時消失/顯現；標註後切換縮圖會存檔，綠勾永遠存在
4. 右欄控制欄空白
5. console `[Intervention] Images loaded lazily...` 警告

#### 設計決策
- **副標題動態化**：`createModal()` 副標題加 `#dataset-manager-subtitle` id，進入標註模式設為 `t('ANNOTATION_MODE_TITLE', '物件偵測標註')`，退出恢復 `t('SUBTITLE', 'Dataset Spec')`
- **純 flex 高度鏈**：標註模式高度鏈改為每層 `flex: 1; min-height: 0`，取代 grid 子項 `height: 100%`（無法解析）
- **annotations 初始化**：後端 `ScanedImage` 無 `annotations` 欄位，前端匯入時初始化每張圖獨立陣列
- **同步初始化畫布**：`UICanvas.init()` 移出 `img.onload`，避免 src 相同時 onload 不觸發

#### 實作內容

##### 第一輪修正
- `ui_layout.js`：
  - 副標題加 `#dataset-manager-subtitle` id；`enterAnnotationMode` 設為「物件偵測標註」，`exitAnnotationMode`（含安全返回分支）恢復「Dataset Spec」
  - `saveCurrentAnnotations()` 改為 `(UICanvas.state.annotations || []).slice()` 拷貝陣列，避免多圖共用參考
  - `loadAnnotationImage()` 將 `UICanvas.init()` 移出 `img.onload`，改為同步初始化
- `dataset_manager.css`：`.dataset-manager-body.dataset-annotation-mode` 加 `flex: 1; min-height: 0`
- `ui_components.js`：標註縮圖欄與縮圖牆的 `<img>` 移除 `loading="lazy"`
- `i18n/zh-hant.js` + `en.js`：新增 `DSM_ANNOTATION_MODE_TITLE`

##### 第二輪修正（綠勾 + 縮圖捲動補強）
- `ui_layout.js`：
  - `handleDirectoryImport` 改為 `state.images = images.map(img => ({ ...img, annotations: img.annotations || [] }))`（後端 `ScanedImage` 無 annotations 欄位）
  - `enterAnnotationMode` 對 overlay 加 `dataset-annotation-fullscreen` class，`exitAnnotationMode` 移除
- `dataset_manager.css`：新增 `.dataset-manager-overlay.dataset-annotation-fullscreen`（`align-items: stretch; padding: 0`）+ 其內 dialog `height: 100%; max-height: 100%; border-radius: 0`（取代不穩定的 `:has()`）

##### 第三輪修正（縮圖捲動關鍵 root cause）
- `dataset_manager.css`：
  - `.dataset-manager-body.dataset-annotation-mode` 由 `display: grid` 改 `display: flex; flex-direction: column; flex: 1; min-height: 0`
  - `.dataset-preview-panel` 由 `height: 100%` 改 `flex: 1; min-height: 0`
  - `#dataset-preview-content` 加 `min-height: 0`
  - `.dataset-annotation-layout` 加 `grid-template-rows: minmax(0, 1fr)`、`flex: 1; min-height: 0`，移除 `height: 100%`
  - `.dataset-annotation-thumbnails` 加 `min-height: 0`

#### 修改檔案清單
| 檔案 | 動作 |
|------|------|
| `ui/src/modules/dataset_manager/ui_layout.js` | 副標題動態化、saveCurrentAnnotations 改 .slice()、UICanvas.init 移出 onload、handleDirectoryImport 初始化 annotations、overlay fullscreen class 加/移除 |
| `ui/src/modules/dataset_manager/dataset_manager.css` | 標註模式高度鏈改純 flex（每層 flex:1; min-height:0）、overlay fullscreen 樣式、grid-template-rows 固定單行 |
| `ui/src/modules/dataset_manager/ui_components.js` | 標註縮圖欄與縮圖牆移除 loading="lazy" |
| `ui/src/modules/dataset_manager/i18n/zh-hant.js` | 新增 DSM_ANNOTATION_MODE_TITLE |
| `ui/src/modules/dataset_manager/i18n/en.js` | 新增 DSM_ANNOTATION_MODE_TITLE |

#### 驗證
- [x] `node --check` 語法檢查通過（ui_layout.js / ui_components.js / i18n zh-hant.js / en.js）
- [x] **Tauri 及 VSIX 雙平台驗證通過**（副標題、縮圖捲動、綠勾、右欄控制欄、console 警告）

---

## 技術深挖 (Technical Deep Dive)

### 縮圖捲動 root cause：grid 子項 height:100% 無法解析
`.dataset-manager-body.dataset-annotation-mode` 原本用 `display: grid`，其子項 `.dataset-preview-panel` 的 `height: 100%` 在 grid 容器（無明確高度）中**無法解析**，導致高度鏈斷裂：
- `.dataset-annotation-layout` 高度 = 內容高度（縮圖很多時被撐高）
- 縮圖欄 `.overflow-y: auto` 因父層無受限高度而不觸發捲動
- 內容超出 dialog 被 `overflow: hidden` 裁切 → 縮圖「不是從第一張開始」

**修正**：改為純 flex 鏈，每層 `flex: 1; min-height: 0`：
```
overlay(dataset-annotation-fullscreen, align-items:stretch)
  → dialog(height:100%)
    → body(dataset-annotation-mode, display:flex; flex-direction:column; flex:1; min-height:0)
      → preview-panel(flex:1; min-height:0)
        → preview-content(flex:1; min-height:0; overflow:hidden)
          → annotation-layout(flex:1; min-height:0; grid-template-rows:minmax(0,1fr))
            → 左縮圖欄/右控制欄 overflow-y:auto; min-height:0 ✅
```

### 後端 images 無 annotations 欄位
`src-tauri/src/commands/file.rs` 的 `ScanedImage` 結構（L306-313）**沒有 `annotations` 欄位**，`pick_folder` 回傳的 images 每張都缺 `annotations`。前端 `state.images = images` 直接指派後，`loadAnnotationImage` 的 `image.annotations || []` 對 undefined 每次建立新陣列但**沒寫回**，導致標註後無處保存、縮圖勾號判斷失效。

**修正**：`handleDirectoryImport` 改為 `state.images = images.map(img => ({ ...img, annotations: img.annotations || [] }))`。

### 綠勾異常：陣列參考共用
`saveCurrentAnnotations()` 原本直接 `state.images[idx].annotations = UICanvas.state.annotations || []`，把**同一個陣列參考**寫入多張圖片 → 某張 push 了 bbox，所有引用它的縮圖都顯示綠勾。

**修正**：改為 `.slice()` 拷貝陣列。

### 右欄空白：onload 回呼鏈未觸發
`UICanvas.init()` 原本放在 `img.onload` 內，當 `img.src` 的 blobUrl 相同時瀏覽器不重載 → `onload` 不觸發 → 右欄控制列表、進度、縮圖高亮、鍵盤綁定全部沒執行。

**修正**：改為同步初始化，onload 只由圖片本身觸發重繪。

---

## 下次啟動方向 (Next Steps)
- 除錯紀錄已完整記錄於 `log/plan/BboxAnnotationUIImprovement.md`（含三輪補強與驗證結果）
- 依用戶指示，此任務未寫入主 `log/todo.md`