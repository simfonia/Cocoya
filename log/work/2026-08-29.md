# 2026-08-29 工作日誌

## [Dataset Manager Refactor] Stage 2 切片 1-5：io/bridge.js Bridge Port（#task[Dataset Manager Refactor - Stage 3]）

### 施工範圍宣告

```text
Stage：Stage 2（Ports、VSIX/Tauri Adapter 與 Sidecar Protocol，依 DatasetManagerRefactorImplementationGuide.md §5）
Owner：Cline (act mode)
允許修改：ui/src/modules/dataset_manager/io/**（新增）、ui_layout.js、sampler.js、parity matrix、backend manifest、FILE_STRUCTURE.md、log/*
禁止修改：window.CocoyaDataset 公開 API、Dataset JSON 格式、spec.js / ui_canvas.js、core/ 五檔行為、Vite classic script warnings、ESLint config
前置 Gate：Stage 1 CLOSED（250f296）；工作樹 clean（HEAD = 01c3efb，標題 Stage 2 實為遠端訓練重構，經使用者決策本輪補做指引 Stage 2）
本階段成功條件：direct Bridge 盤點 + 遷移至 io/bridge.js；requestId/timeout/cancel/unsubscribe 測試通過；A2-1~A2-4 手動驗證
預計驗證命令：node --check、node --test io/bridge.test.mjs、npm run build --prefix ui
需要人工測試的平台：VSIX + Tauri Dev（A2-1~A2-4）
```

### 背景決策

- 指引上的 Stage 2（Ports/Adapter/Sidecar Protocol）先前未施工（commit `01c3efb` 標題 Stage 2 實為遠端訓練重構）。使用者決策：先補 Stage 2，依序列推進。
- 遠端訓練重構 D2 已移除 DM 雲端 ZIP 上傳按鈕與前端流程 → 指引手動案例 A2-5/6/7 標 N/A；parity matrix `datasetUploadArchive` 等項目不列入本階段成功條件。
- Tauri `_dispatchToFrontend` 同時走 `_listeners`（onMessage）與 `window.postMessage`（相容），故 `io/bridge.js` 包 `CocoyaBridge.onMessage` 即同時涵蓋 VSIX（postMessage）與 Tauri（invoke/event）。

### 異動（依切片）

| 切片 | 檔案 | 內容 |
|------|------|------|
| 1 | `io/bridge.js`（新增） | Dataset Manager 唯一 Bridge Port：`send` / `subscribe`(回傳 unsubscribe) / `subscribeMultiple` / `once` / `request`（requestId 精準配對或 per-command FIFO、timeout、cancel、SEND_FAILED） / `dispose`；typed 捷徑轉發（confirm/prompt/alert/pickFolder/prepareDatasetImport/loadDatasetProgress/saveDatasetProgress/getProjectAnchor/getCapabilities）；`createDatasetBridge(transport)` 可注入 fake transport |
| 1 | `io/bridge.test.mjs`（新增） | fake transport 測試 8 條：正確 resolve、FIFO 不交叉、requestId 亂序精準配對、timeout 清理 + 遲到回應丟棄、cancel、unsubscribe、once、dispose |
| 2 | `ui_layout.js` | `loadProgressFromFolder`、`writeProgressToDisk`（autosave）改 `datasetBridge.request()`（timeout 15s） |
| 3 | `ui_layout.js` | `handleExportDataset` 改 `request`（timeout 120s，含 ZIP 打包）；`handleDeleteImage` send 改經 datasetBridge |
| 4 | `sampler.js` | `init()` 訂閱改 `datasetBridge.subscribe`（新增 `dispose()`）；`listCameras`/`startCamera`/`takeSnapshot` 改 `request`（takeSnapshot 保留原 requestId 精準配對，timeout 15s）；`stopCamera` 改 `send`；移除所有裸 `window.addEventListener('message')` |
| 5 | `ui_layout.js` | `checkRemoteEnvironment` send 改經 datasetBridge；diagnostic 全域 dispatcher（checkRemoteEnvironmentResult/datasetUploadResult）改 `subscribeMultiple`（回傳 unsubscribe，供 Stage 4 生命週期接管）；confirm/prompt/alert/pickFolder/prepareDatasetImport/getProjectAnchor/capabilities 共 18 處改 typed 捷徑 |

### 技術深挖 (Technical Deep Dive)

- **correlation 雙策略**：後端有回塞 `requestId` 的事件（`datasetCaptureResult`、`projectAnchorResult`）用精準配對；沒有的（`datasetSave/LoadProgressResult`、`datasetExportResult`、`datasetCameraListResult`）用 per-command FIFO（單一通道有序，先發先配）。autosave 重疊請求因此安全：FIFO 依發送序配對，不會 A 回應套到 B。
- **pending 先註冊後 send**：`request()` 在 `transport.send()` 前先把 pending entry 放進 map，消滅「回應早於 listener 註冊」的 race（sampler startCamera 原版有此隱患）。
- **timeout 鐵律**：timeout/cancel/dispose 一律從 pending map 移除 entry 並 `clearTimeout`，遲到回應落在空 map 直接丟棄——不會有幽靈 callback，也不會 resolve 已 reject 的 promise。
- **static gate**：`dataset_manager/*.js` 內 `window.CocoyaBridge` 殘留 = 0（grep 驗證）。`bridge.js` 本身的 default transport 是唯一存取點。
- **不動契約**：所有 command 名稱、payload 欄位、result event 名稱完全未變，後端零修改（Rust/TS 不需重編）。

### 驗證

- `node --check`：bridge.js / ui_layout.js / sampler.js 全 PASS
- `node --test io/bridge.test.mjs`：8 pass / 0 fail
- `npm.cmd run build --prefix ui`：PASS（built in ~160ms）
- `git diff --stat`：僅 sampler.js / ui_layout.js + 新增 io/ 兩檔；後端與 TS 無異動

### 手動測試狀態（READY_FOR_TEST）

| 案例 | 平台 | 狀態 |
|---|---|---|
| A2-1 command parity（pick folder / load / save / delete / export / camera list-start-stop-capture） | VSIX + Tauri Dev | 待測 |
| A2-2 非同步 correlation（連續兩次 capture / autosave 重疊）| VSIX + Tauri Dev | fake transport 自動化部分 PASS（8 測試）；實機待測 |
| A2-3 多視窗 sidecar 隔離 | Tauri Dev | 待測 |
| A2-4 權限與錯誤路徑 | Tauri Dev（Release 仍 BLOCKED，沿既有） | 待測 |
| A2-5/6/7 雲端上傳 | - | N/A（上傳按鈕已於 RemoteTrainingRefactor D2 移除） |

### FAIL / BLOCKED

- Tauri Release 安裝版驗證：BLOCKED（沿既有，非本階段新增）。
- SSH/SFTP 遠端驗證：BLOCKED（主機連線不穩，非本階段範圍；不得標 PASS）。

## AI Agent Handoff（Stage 2 切片 1-5）

- 日期：2026-08-29
- Agent：Cline
- 階段：Stage 2（io/bridge.js port 層完成；guide Stage 2 Gate 未全過）
- 狀態：READY_FOR_TEST
- HEAD：`01c3efb`（本輪修改未 commit）
- 工作樹：dirty（本 agent 修改：io/bridge.js、io/bridge.test.mjs 新增；sampler.js、ui_layout.js、FILE_STRUCTURE.md、DatasetManagerParityMatrix.md、todo.md）
- 本次目的：補做指引 Stage 2——Bridge port 集中化 + correlation/timeout/cancel/dispose
- 已完成：切片 1-5（見上表）；direct `window.CocoyaBridge` 殘留 0
- 修改檔案：如上
- 未完成或風險：A2-1~A2-4 手動雙平台待測；`Sampler.dispose()` 尚未於 modal 關閉流程呼叫（Stage 4 生命週期接手）
- 測試命令與結果：node --test 8/8 PASS；node --check PASS；vite build PASS
- 手動案例與結果：待使用者執行 A2-1~A2-4
- 證據位置：本檔 + DatasetManagerParityMatrix.md Verification log 2026-08-29 列
- 文件同步狀態：parity matrix、FILE_STRUCTURE.md、todo.md 已同步；backend_api_manifest 無契約變更故未動
- 下一個 agent 第一動作：等使用者回報 A2-1~A2-4 結果 → 全 PASS 才可簽核 Stage 2 Gate 進入 Stage 3（Application Use Cases）；若 FAIL，修正後重跑同切片驗證
- 禁止重做或修改的事項：不得改 `window.CocoyaDataset` API、不得刪 ui_layout.js、不得將 SSH/Release BLOCKED 標 PASS、不得動 Vite warnings/ESLint config
- 需要產品決策的問題：無
- 交接者：Cline

## 下次啟動方向 (Next Steps)

### [追加] 2026-08-29：commit 01c3efb 標題更正

- 依使用者指示，將原標題「Dataset Manager Refactor - Stage 2」的 commit（實為遠端訓練重構）以 `git commit --amend` 更名為「遠端訓練重構」，新 hash `0709e2e`；已 `git push --force-with-lease origin master` 同步至 GitHub（01c3efb → 0709e2e forced update）。
- 本輪 Stage 2 的 7 個未提交修改未被併入 amend（index 為空確認後才執行），工作樹仍 dirty。
- 以上 Handoff 中 HEAD 欄位請以 `0709e2e` 為準。

- 使用者執行 A2-1~A2-4 手動測試（VSIX + Tauri Dev），結果回報後決定 Stage 2 Gate 簽核與否。
- Gate 通過後進 Stage 3：Application Use Cases（save/load、autosave、import/export、annotation mutation 從 ui_layout.js 抽為 use-case；cloud upload 已移除標 N/A）。
