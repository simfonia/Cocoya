# Cocoya 工作日誌 (2026-04-26)

## 任務摘要
- **[里程碑] 雙模韌體重置系統 (Reset Firmware)**：
    - 實作 Strategy A (UF2 模式)：支援 Maker Pi RP2040 與 XIAO S3 的一鍵恢復。
    - **自動化初始化**：燒錄後自動進入 15 秒磁碟輪詢，偵測到 `CIRCUITPY` 後自動寫入乾淨的 `code.py`。
    - **自訂韌體支持**：允許使用者選取自行準備的 `.uf2`，並提供「可選式清空」選項以保護特殊韌體內容。
- **通訊橋樑 (Bridge.js) 深度重構**：
    - **多重監聽機制**：改用 `Set` 管理監聽器，解決 `confirm/prompt` 對話框回調互相覆蓋的 Bug。
    - **結構正規化**：統一 `send` 函式結構，確保每個指令都明確處理 VS Code (`postMessage`) 與 Tauri (`tauriInvoke`) 雙路徑。
- **穩定性與細節修復**：
    - **排除 SyntaxError**：在 `module_loader.js` 加入腳本重複載入檢查，並將積木/產生器中的全域 `const` 變數降級為 `var`。
    - **回歸邏輯補救**：透過 `git diff` 補回意外遺失的「上次路徑記憶」、「環境驗證提示」與「狀態列路徑同步」功能。

## 詳細異動
1. **src-tauri/**:
   - `lib.rs`: 實作 `reset_firmware` 指令。具備磁碟標籤掃描 (`RPI-RP2`)、UF2 複製、磁碟重啟輪詢與 `code.py` 自動寫入功能。
2. **src/extension.ts (VSIX Backend)**:
   - 實作 `handleResetFirmware`：使用 `child_process` 掃描磁碟並執行 UF2 燒錄流程。
   - 實作 `handlePickMcuModel`：對接 VS Code 原生 `QuickPick` 選單，提供更專業的型號選取體驗。
   - 修復 TypeScript 型別錯誤 (QuickPickItem casting) 與邏輯回歸點。
3. **ui/src/**:
   - `bridge.js`: 深度重構分發結構。新增 `confirm`, `prompt`, `alert`, `pickMcuModel` 的 Promise 封裝介面。
   - `ui_manager.js`: 更新設定選單綁定，對接最新的型號選取與燒錄詢問流程。
   - `module_loader.js`: 加入 `document.querySelector` 檢查，防止腳本標籤重複注入。
   - `zh-hant.js`, `en.js`: 新增 `MSG_ASK_CLEAR_CODE`, `MSG_SELECT_MCU_MODEL` 等 5 組韌體相關翻譯。
   - `modules/`: 修改 `hardware_blocks.js` 與 `mcu_car_generators.js` 的變數宣告以避免重定義錯誤。
4. **Resources**:
   - 建立 `resources/firmware/CircuitPython/` 下的型號子目錄結構。
   - 更新設定選單圖示為 `hr_resting_24dp_FE2F89.png`。

## 待辦事項
- [ ] 整合 `esptool` Sidecar 以實作 Strategy B (Serial 模式燒錄)。
- [ ] 考慮加入燒錄進度百分比顯示（若底層工具支援）。
- [ ] 實作 Tauri 模式下的環境診斷 UI。
