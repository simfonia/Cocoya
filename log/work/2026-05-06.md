# 工作紀錄 2026-05-06

## 今日重點 (Refactoring Phase: UI Logic Modularization)
- **架構重構：模組化 `ui_manager.js` 與 `main.js`**
    - 啟動 `CocoyaApp` / `CocoyaUI` 系統性重構，解決單一檔案過大（Monolithic）導致的維護困難。
    - **[SUCCESS] 實作 `ui/terminal.js`**：從 `ui_manager.js` 抽離終端機控制邏輯，包含自動捲動、內容追加與面板切換。
    - **[SUCCESS] 實作 `ui/renderer.js`**：抽離 Python 預覽渲染、積木同步高亮、以及預覽面板的佈局控制（縮放與收合）。
    - **[FIX] 解決物件覆寫問題**：將 `window.CocoyaUI` 初始化改為 `Object.assign` 模式，確保多個模組可安全地合併到全域物件中，避免 `toggleTerminal is not a function` 報錯。
    - **[FIX] 修正 Vite 編譯報錯**：修復 `ui_manager.js` 修改後遺漏的閉合括號語法錯誤。

## 異動檔案
- `ui/index.html`: 新增 `renderer.js` 載入路徑，調整載入順序。
- `ui/src/ui/terminal.js`: **[NEW]** 終端機邏輯模組。
- `ui/src/ui/renderer.js`: **[NEW]** 預覽渲染與佈局模組。
- `ui/src/ui_manager.js`: 移除已遷移的邏輯，更新初始化模式。
- `docs/api_manifest.md`: **[NEW]** 作為重構對齊的 SSOT API 清單，紀錄所有公共方法的遷移進度。

## 待辦事項
- 繼續抽離 `CocoyaUI` 的硬體控制（Serial/Firmware）至 `ui/hardware.js`。
- 抽離對話框邏輯（Modal/FlashButton）至 `ui/dialogs.js`。
- 開始 `main.js` (CocoyaApp) 的模組化，將 Workspace 管理、設定、Persistence 獨立化。

## 模組化重構持續推進 (Modularization Phase 2)
- **[SUCCESS] 實作 ui/hardware.js**：抽離序列埠管理、執行按鈕 Tooltip 更新、韌體設定選單控制以及環境診斷顯示。
- **[SUCCESS] 實作 ui/dialogs.js**：抽離 QuickPick、Loading Modal、閃爍反饋 (FlashButton) 以及高亮顏色設定。
- **[SUCCESS] 實作 ui/base.js**：整合剩餘的 UI 狀態 (Dirty/Filename)、i18n 套用、更新狀態顯示，以及最核心的 initToolbar 事件綁定邏輯。
- **[SUCCESS] main.js (CocoyaApp) 深度解耦**：
    - 將原本 700+ 行的單一檔案拆分為 pp/ 下的四大模組：config.js (配置), persistence.js (持久化), workspace.js (Blockly 管理), lifecycle.js (生命週期)。
    - main.js 現在僅作為乾淨的啟動進入點 (Entry Point)。
- **[SSOT] 文檔同步**：完成 pi_manifest.md 的所有狀態更新，確保 100% 覆蓋已遷移的公共 API。

## 異動檔案
- ui/index.html: 更新載入順序，加入 hardware.js, dialogs.js, ase.js 以及 pp/*.js。
- ui/src/ui/hardware.js, ui/src/ui/dialogs.js, ui/src/ui/base.js: **[NEW]** UI 功能模組。
- ui/src/app/config.js, ui/src/app/persistence.js, ui/src/app/workspace.js, ui/src/app/lifecycle.js: **[NEW]** 應用程式核心模組。
- ui/src/ui_manager.js, ui/src/main.js: 核心邏輯遷移完成，轉換為 Legacy Wrapper/Entry Point。
- docs/api_manifest.md: 更新遷移狀態為 "Migrated"。

## 多視窗穩定性與完整性協議 (Multi-Window Integrity Protocol Phase)
- **[重大修復] 實作 MWIP 鎖定機制**：
    - 在 Rust 端建立 `file_locks` 映射表，實現「首開鎖定，後開唯讀」的安全策略。
    - **[FIX] 唯讀模式智慧化**：允許唯讀視窗變髒（顯示 `*`），並在關閉時引導至「另存新檔」，確保資料不丟失。
- **[重大修復] 解決競態與廣播衝突**：
    - **[FIX] 關閉廣播干擾**：改用 `emit_to` 單播技術，確保 B 視窗按 `X` 時不會同時觸發 A 視窗的詢問框。
    - **[FIX] 關閉對話框重疊**：透過 `await` 髒狀態同步，確保 Rust 攔截器不再發出重複的關閉請求。
- **[重大修復] 宣示權備份管理**：
    - 實作 `.recovering` 原子化重新命名，保證無主備份只會被單一視窗領取，徹底解決多視窗爭搶備份的 Bug。
- **[文檔化] 規格固化**：將 MWIP 協議正式寫入 `GEMINI.md` 與 `system_spec.html` (Section 23)。

## 異動檔案
- `src-tauri/src/lib.rs`: **[MAJOR]** 實作 `file_locks` 鎖定、`check_startup_backup` 宣示邏輯與單播事件。
- `ui/src/bridge/tauri.js`: 補齊 `is_read_only` 傳遞，優化 `_handleCloseDialog` 狀態同步。
- `ui/src/app/persistence.js`: 解除唯讀模式的 `setDirty` 限制，加入存檔成功後的權限轉正邏輯。
- `GEMINI.md`, `docs/system_spec.html`: 更新多視窗開發鐵律與系統規格。

