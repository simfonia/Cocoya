## [2026-04-24] 主題自動同步與 Minimap 視覺修復

### 1. 自動主題同步 (Auto Theme Sync)
- **實作機制**：在 ui/src/main.js 中實作 setupThemeSync，透過 MutationObserver 監聽 VS Code 的 ody 類別變動（.vscode-dark），並結合 matchMedia 支援 Tauri 獨立模式的系統偏好偵測。
- **錯誤修復**：修正了 setTheme 傳遞字串導致的 getComponentStyle is not a function 崩潰問題。現在會動態 
ew Blockly.Theme 實例以符合 Blockly 核心要求。

### 2. 深色模式視覺優化 (CSS Overrides)
- **背景修正**：強制覆寫 .blocklyMainBackground 與 .blocklyFlyoutBackground 為 #1e1e1e，解決主題切換時背景依然呈現白色的問題。
- **代碼預覽**：為 Python 預覽區域加入了符合 VS Code Dark 風格的 hljs (Highlight.js) 語法高亮配色。
- **Toolbox 優化**：調整了深色模式下的文字顏色與邊框。

### 3. Minimap 穩定性與同步修復
- **NaN 防護盾**：在 initMinimap 中攔截 getContentMetrics 的異常值，防止因內容未就緒導致的渲染崩潰。
- **孤兒狀態同步**：修正了 	riggerBlockStateUpdate，現在主工作區積木變為孤兒（變灰）時，會同步尋找並更新 Minimap 中對應的積木狀態，確保視覺一致性。
- **效能優化**：移除了主題切換與積木移動時不必要的 efreshMinimap (Full Refresh) 調用，改依賴增量事件鏡像，徹底解決 Minimap 更新時的抖動與閃爍問題。

### 4. 其他修正
- **孤兒檢測邏輯**：在 	riggerBlockStateUpdate 中加入了對 setDisabledReason 的安全檢查，確保相容不同版本的 Blockly 核心。
