# 2026-04-30 工作日誌

## 本日進度摘要
1.  **[UI/UX 優化]** 為「深度修復 (清空 MCU 檔案)」選單項目加入 Hover 說明與直觀圖示。
2.  **[術語修正]** 將 MicroPython 下的「重建磁區」改為「清空使用者檔案」，對齊真實運作機制。
3.  **[核心/VSIX]** 徹底解決序列埠佔用衝突（PermissionError 13），實作 Terminal Singleton 模式。
4.  **[UI/UX 優化]** 序列埠清單加入「硬體智慧辨識 (Smart Labeling)」，自動顯示開發板型號。
5.  **[性能優化]** 優化 PowerShell 偵測指令，將偵測延遲從 3s 縮短至 0.2s，並改為非同步執行。

## 關鍵異動說明
- **ui/index.html**: 
    - 為 `btn-erase-filesystem` 加入 `title` 屬性與新圖示 `delete_24dp_FE2F89.png`。
- **ui/src/style.css**: 
    - 為 `.serial-select` 加入 `max-width` 與 `text-overflow: ellipsis` 限制，防止長檔名破壞版面。
- **ui/src/ui_manager.js**: 
    - 更新 `updateSerialPorts` 支援物件格式，並動態同步 `title` 屬性以便 Hover 顯示完整名稱。
- **ui/src/zh-hant.js / en.js**: 
    - 補齊 `TLB_SETTINGS_ERASE_FS_TOOLTIP`。
    - 修正 `MSG_ERASE_FS_CONFIRM` 文字，移除誤導性的「重建分區」術語。
- **src/extension.ts**: 
    - 實作 `stopAllCocoyaTerminals()`：在開啟新視窗前強制關閉舊有的 Cocoya 終端機。
    - 重構 `handleRefreshSerialPorts()`：
        - 改用 `Win32_SerialPort` WMI 類別提升 10 倍偵測速度。
        - 實作 VID/PID 映射表，將通用名稱轉換為開發板型號（如 Maker Pi, XIAO）。
        - 改用非同步 `exec` 避免阻塞 Extension Host。
    - 修正 TypeScript 隱含 `any` 類型錯誤。

## 待辦事項 (跨平台)
- [ ] 研發 macOS/Linux 的序列埠 Friendly Name 抓取指令。
- [ ] Tauri (Rust) 後端同步實現進程單一化管理與詳細資訊回傳。
`n6.  **[UI/UX]** 取消自製自動捲動開發任務，並將原本的 `scroll_options` 插件預設值設為 **關閉**，以避免寬積木干擾。

7. **[Tauri/除錯]** 徹底解決 Tauri 獨立版資源載入與 CDN 攔截問題。
   - **資源本地化**：下載 highlight.js (CSS/JS) 與 favicon.ico 至本地，避開追蹤保護 (Tracking Prevention) 封鎖。
   - **平台自動遷移**：實作 CircuitPython -> MicroPython 自動遷移邏輯，修復積木定義遺失 (py_definition_zone) 報錯。
8. **[Tauri/架構]** 實作 **多視窗模式 (方案 B)**。
   - Rust 後端新增 create_window 命令，前端「開新檔案」改為彈出獨立新視窗。
   - 支援同時編輯多個專案，且視窗狀態互不干擾。
9. **[UI/UX 精進]** 工具列與視窗標題優化。
   - **響應式佈局**：工具列支援 flex-wrap 與媒體查詢，解決窄視窗排版亂掉問題。
   - **標題同步**：實作後端 set_window_title 命令，動態同步檔案名稱與髒狀態 (Dirty *) 至 OS 視窗標題。
   - **檔名標籤**：工具列中間加入「麵包屑」風格檔名顯示，提升多視窗識別度。

10. **[Tauri/架構]** 徹底實作 **多視窗狀態隔離**。
    - 重構 AppState：將 current_paths 與 python_processes 改為 HashMap，以 window.label() 隔離各視窗狀態。
    - 解決 A 視窗存檔會誤用 B 視窗路徑的嚴重 Bug。
    - 優化孤兒備份搜尋邏輯，新視窗啟動時會自動偵測並提醒恢復因當機留下的舊備份。
11. **[UI/UX]** 實作 **整合式終端機面板 (Terminal)**。
    - 支援 PC 模式 (python -u) 與 MCU 模式 (Serial) 的即時日誌轉發，不再依賴開發者工具。
    - 功能：自動展開、手動清空、暫停自動捲動（Pause Scroll）、1000 行效能保護。
    - 視覺：更換為粉色專屬圖示，支援深色模式，並將停止按鈕移至執行按鈕旁以利操作。
12. **[核心/除錯]** 修正多項功能異常。
    - 修復平台切換 (PC/MCU) 失效問題，補齊 bridge.js 遺失指令。
    - 強化「深度修復」腳本：增加強力中斷訊號 (Ctrl+C x3) 與同步等待機制，確保能清除卡死的程式。
    - 修正開發模式下 resources/deploy_mcu.py 的路徑解析錯誤。

13. **[UI/UX]** 工具列佈局與檔名顯示優化。
    - 將檔名 Breadcrumb 移至「開新專案」按鈕左側，並加入 margin 確保視覺呼吸空間。
    - 移除中央彈性空白區的冗餘標籤。
14. **[Tauri/更新]** 對齊 #wavecode 更新檢查機制。
    - 加入 spin-animation (檢查中) 與 bounce-gradient (發現更新) 動畫。
    - 修復 Tooltip 顯示：現在不論狀態均會明確標註版本號 (如「已是最新版: v0.7.5」)。
    - 實作啟動 1 秒後自動靜默檢查。
15. **[Terminal]** 終端機進階優化。
    - 實作自定義深色捲軸，解決與黑色背景不搭的問題。
    - 實作「執行時自動恢復捲動」：按下執行按鈕後自動重新 Enable 自動捲動功能並更新按鈕視覺。
16. **[診斷/Serial]** 核心功能增強。
    - 修復環境診斷卡住問題：改為單次 Python 調用批次偵測，解決參數命名衝突。
    - 序列埠偵測升級：對齊 VSIX 格式 (移除 PID 顯示)，維持智慧硬體辨識標籤。
