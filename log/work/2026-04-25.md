# Cocoya 工作日誌 (2026-04-25)

## 任務摘要
- **Tauri 核心指令補完**：實現序列埠偵測、MCU 部署、檔案儲存/讀取，以及實體自動備份後端對接。
- **實體化自動備份系統**：
    - 捨棄 localStorage，改用後端實體存檔（VSIX 與 Tauri 雙模式支援）。
    - 實作「跟隨專案路徑」機制：已存檔專案備份於專案旁（隱藏檔 .bak），未存檔則存於系統暫存區。
    - 實作「備份封存分叉（Forking）」保護：拒絕恢復時自動重命名封存，防止心血被後續更動覆蓋。
- **UI 與體驗強化**：
    - 將設定按鈕升級為下拉選單，並引入官方 Python 圖示 (`python.png`)。
    - 修正深色主題適配、下拉選單穩定性、以及啟動時平台選擇器同步問題。
- **VSIX 穩定性優化**：修正 VS Code 模態對話框重複取消按鈕的問題，補齊語系鍵值。
- **程式碼維護**：更新 Blockly 棄用 API，確保長期相容性。

## 詳細異動
1. **src-tauri/**:
   - `Cargo.toml`: 新增 `serialport` 依賴。
   - `src/lib.rs`: 
     - 實作 `get_serial_ports` (Rust 原生支援)。
     - 實作 `deploy_mcu` (對接 Python 腳本)。
     - 實作 `auto_backup`, `clear_backup`, `reject_recovery` 指令。
     - 強化 `open_file` 與 `get_manifest` 的備份偵測與推播邏輯。
2. **src/extension.ts (VSIX)**:
   - 實作實體備份處理邏輯 (`handleAutoBackup`, `handleClearBackup`, `handleRejectRecovery`)。
   - 修正 `handleConfirm` 移除多餘取消按鈕。
   - 在專案開啟與 IDE 啟動時加入主動備份檢查與推播。
3. **ui/src/**:
   - `bridge.js`: 完整對接備份、檔案操作與對話框指令（支援雙模式判斷）。
   - `main.js`: 
     - 實作 Debounce 2秒的自動備份發送。
     - 實作恢復對話框與分叉保護邏輯。
     - 修正啟動時平台同步 (selector.value) 與主題初始化。
     - 更新 `localStorage` 持久化平台選擇。
   - `zh-hant.js`, `en.js`: 補齊 `MSG_OK`, `MSG_RECOVER_*` 等翻譯鍵值。
   - `style.css`: 新增下拉選單樣式、深色模式樣式，以及 CSS 偽元素隱形橋樑。
   - `modules/core/variables/variables_blocks.js`: 改用 `getVariableMap().getAllVariables()` 以對應 Blockly 新版 API。

## 待辦事項
- [ ] 整合 `deploy_mcu.py` 作為 Tauri Sidecar，移除對環境 Python 的直接依賴。
- [ ] 實現 Tauri 模式下的環境診斷功能 (`check_environment`)。
- [ ] 評估是否需要備份檔的自動清理週期（例如保留最近 N 個封存檔）。
