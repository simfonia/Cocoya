# 2026-04-29 工作日誌

## 本日進度摘要
1.  **[重大架構轉型]** 決定放棄不穩定的 CircuitPython (USB MSC) 模式，全面遷移至 **MicroPython (Serial REPL)** 模式。
2.  **[部署工具重構]** 徹底重寫 `resources/deploy_mcu.py`，移除磁碟掃描邏輯，改為全序列埠 Raw REPL 檔案推送協議。
3.  **[硬體產生器 (HAL) 遷移]** 完成核心硬體積木由 `board/digitalio` 向 `machine` 庫的轉換。
4.  **[UI/UX 優化]** 重新設計「韌體設定」子選單，加入「鎖定/重設/重置」功能群組，並針對 MicroPython 進行介面清理。

## 關鍵異動說明
### 1. 底層通訊與部署 (`deploy_mcu.py`)
- 實作了 MicroPython Raw REPL 協議的上傳流程（煞車 -> 進入 Raw 模式 -> 指令推送代碼 -> 軟重啟）。
- 代碼儲存檔名由 `code.py` 變更為 `main.py`。
- 新增 `--monitor-only` 與 `--erase-filesystem` 模式，支援序列埠監控重連與 Flash 深度擦除。

### 2. 硬體產生器遷移 (`ui/src/modules/`)
- **mcu_car_generators.js**: 重寫了馬達、伺服機、超音波、音樂引擎等驅動邏輯，全面使用 `machine` 模組。
- **io_generators.js**: 遷移序列埠讀寫邏輯，改用標準 `print` 與 `sys.stdin` 互動。
- **time_generators.js**: 補齊了 MicroPython 缺失的 `strftime` 輔助函式。
- **mcu_camera/huskylens**: 更新為 MicroPython 特有的 I2C 與 Camera 初始化語法。

### 3. 前端介面與語系 (`ui/src/`)
- **ui_manager.js**: 重新整理工具列按鈕與設定選單，移除 CircuitPython 補丁開關。
- **zh-hant.js / en.js**: 更新所有顯示標籤為 "MicroPython"，並補齊子選單翻譯。
- **style.css**: 實作了巢狀子選單的 Hover 互動效果與深色模式適配。

### 4. VSIX 後端整合 (`src/extension.ts`)
- 加入 `stopAllCocoyaTerminals` 輔助函式，防止執行硬體任務時 COM 埠被佔用。
- 更新平台偵測邏輯，確保舊有 XML 檔案能正確識別並載入。

## 檔案結構變動 (FILE_STRUCTURE.md 同步更新)
- `resources/deploy_mcu.py`: 改為 MicroPython 全序列埠傳輸版本。
- `ui/src/modules/`: 所有 `*_generators.js` 均已更新為 MicroPython 語法。
- `log/todo.md`: 新增里程碑 v6.0。

---
*備註：今日經歷了從 CircuitPython 到 MicroPython 的重大決策轉變，實測結果顯示序列埠傳輸模式在 Windows 教學環境下具備極高的穩定性。*

## Gemini 補強紀錄
- **[補強] 基礎硬體產生器遷移**：補完 hardware_generators.js 的 MicroPython 遷移。將 digitalio, pwmio, nalogio 全面更換為 machine 模組，並實作了相容 Pico 與 XIAO S3 的腳位映射邏輯。
- **[同步] 任務清單更新**：更新 	odo.md 以對齊最新的里程碑進度。

- **[修復] 數學模組 MicroPython 相容性**：修正 math_generators.js 中 andom.seed() 使用 	ime.monotonic_ns() 的問題。改採 	ime.ticks_us() (MicroPython 專用) 並保留對一般 Python 的相容性。

- **[優化] 部署流程體驗**：調整 deploy_mcu.py，將「上傳成功」訊息提前至程式啟動前顯示，並加入 1 秒停頓，確保使用者能先看到成功提示。

- **[優化] 部署訊息時序精確化**：修改 monitor 函式，透過偵測序列埠流中的 soft reboot 字串來精確注入上傳成功橫幅，確保其出現在韌體啟動後、使用者程式執行前。
