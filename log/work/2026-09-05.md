# 2026-09-05 工作日誌

## 任務：terminal UI 新增功能（字體大小 / 複製全部 / 高度拖曳 / 三角形收合把手）

### 需求
1. 終端機字體大小設定（icon: `font-size.png`）
2. 複製全部文字到剪貼簿（icon: `copy-duotone.png`）
3. 上下拖曳調整終端機高度 + 三角形收合把手（對齊程式碼預覽 `#panel-resizer` / `#code-toggle` 風格）

### 討論決策
- **主題連動**：終端機內容區維持「永遠深色」（VS Code 慣例，現況即寫死 `#1e1e1e`，三主題皆未覆寫），不主題化；僅新的三角形把手 `#terminal-toggle` 需主題連動（貼在面板邊界會暴露於主題背景），比照 `#code-toggle` 補 `body.vscode-dark / cocoya-dark-mode` 深色規則。拖曳棒 hover 用品牌色 `#FE2F89` 三主題通用。
- **字體大小：終端機與程式碼預覽分開**（共用機制、分開狀態）：用途不同（投影教學放大 vs 對照積木行號）、預設值不同（12px vs 13px）、各自 localStorage 記憶；共用同一大小會讓 code 面板（固定 400px 寬）爆版面。

### 異動檔案
| 檔案 | 內容 |
|------|------|
| `ui/index.html` | ① `#blocklyArea` 內 terminalArea 前新增 `#terminal-toggle`（三角形把手，absolute）與 `#terminal-resizer`（高度拖曳棒，flow 內 6px/上下 margin -3px）② terminalHeader 新增 `btn-copy-terminal`、`btn-fontsize-terminal` ③ codeHeader 新增 `btn-fontsize-code` |
| `ui/src/style.css` | ① `#terminalArea.collapsed` height 加 `!important`（否則使用者拖曳後的 inline height 蓋過收合）② 新增 `#terminal-toggle` / `#terminal-resizer` 樣式與 `body.resizing-terminal`（overlay 顯示、關閉 transition、row-resize 游標）③ 深色主題選擇器群組（654 行處）加入 `#terminal-toggle` |
| `ui/src/ui/terminal.js` | ① initTerminal 擴充：複製全部（`navigator.clipboard.writeText` + `flashButton` 綠閃）、把手 onclick、高度拖曳（mousedown/raf 節流 mousemove/收尾 svgResize+resize event，範圍 100px ~ innerHeight-200px）② 新增 `UI.setupFontSizeCycler(btnId, targetId, sizes, storageKey)`（共用循環切換 + localStorage 記憶，終端機 12/14/16/18、程式碼 13/15/17）③ 新增 `UI.syncTerminalToggle()`（把手 bottom = 終端機 offsetHeight，箭頭 ▲收合/▼展開），`toggleTerminal` 內呼叫 |
| `ui/src/zh-hant.js` / `en.js` | 新增 key：`TLB_TOGGLE_TERMINAL`、`TLB_COPY_TERMINAL`、`TLB_FONT_SIZE_TERMINAL`、`TLB_FONT_SIZE_CODE`、`TLB_DRAG_RESIZE_HEIGHT` |

### 技術深挖 (Technical Deep Dive)
- **把手不能放在 `#terminalArea` 內部**：`.collapsed` 有 `opacity:0; pointer-events:none` 且 `overflow:hidden`，內部把手會被隱藏/裁切（2026-05-03 `#code-toggle` 曾踩過同類坑，該案例是靠 collapsed 時保留把手可見解決；terminal 版直接放外部作兄弟節點更乾淨）。`#blocklyArea` 為 `position:relative` flex column，把手 absolute 錨 `bottom`，由 JS `syncTerminalToggle()` 同步 = 終端機 offsetHeight，展開時貼在終端機上緣、收合時貼底。
- **inline height 與收合衝突**：拖曳後 `panel.style.height` 為 inline style，會蓋過 `.collapsed { height:0 }` → 收合失效；解法為 `height:0 !important`，重新展開時 inline height 自動恢復使用者偏好尺寸。
- **拖曳沿用 `#panel-resizer` 慣例**：`requestAnimationFrame` 節流、`#resizer-overlay`（fixed 全螢幕）擋 mouseover 干擾（新增 `body.resizing-terminal` 觸發）、mouseup 後 `Blockly.svgResize()` + `window resize` 事件讓畫布重新適配。
- **字體記憶**：`localStorage` key `cocoya_terminal_fontsize` / `cocoya_code_fontsize`，init 時還原套用。

### 驗證
- `node --check` terminal.js / zh-hant.js / en.js 全過
- `npx vite build`（ui/）✓ built in 366ms
- [ ] 實機待測（VSIX + Tauri）：字體切換與重開記憶、複製、拖曳高度與 Blockly 同步、三角形收合/展開、深淺主題下把手外觀

### 下次啟動方向 (Next Steps)
- 實機驗證上述清單；若使用者希望兩面板字體同步或 terminal 內容主題化（cssVars），依本日誌討論決策章節重新評估。

## 追記：把手位置 Bug 修正（同日）
- **症狀**：把手位置顛倒——展開時在面板最下方、收合時在上方。
- **根因**：`syncTerminalToggle()` 在 `toggleTerminal()` 切換 class 後立即讀 `panel.offsetHeight`，但 `#terminalArea` 高度有 0.3s transition，當下量到的是**舊值** → bottom 套到錯誤高度。
- **修正（依使用者需求改為「面板上緣水平置中」）**：捨棄 JS 同步位置的方案，改把 `#terminal-toggle` 移入永遠存在於版面中的 `#terminal-resizer`（加 `position:relative`），絕對定位 `left:50%; translateX(-50%); bottom:6px`（40×16 橫向膠囊、圓角朝上），位置完全由 CSS 錨定，收合/展開皆正確。`syncTerminalToggle()` 只負責箭頭方向；`resizer.onmousedown` 加 `e.target.closest('#terminal-toggle')` 防止點把手誤觸拖曳。
- **箭頭方向**：使用者確認原語意正確（收合=▲、展開=▼），已改回。
- 驗證：`node --check` + `npx vite build` 全綠。

## 追記：codeHeader 關閉鈕 hover 無變色（同日）
- 根因：`#btn-close-code` 的「×」是純文字 `<span>`，既有 hover 規則只覆蓋 `.toolbar-btn` 背景與 `img`（`filter` 轉品牌色），文字色不變；且淺色主題 hover 底色 `#f0f0f0` 與 `--code-header-bg #f3f3f3` 過近，肉眼幾乎無感。
- 修正：style.css 新增 `#btn-close-code:hover span { color: #FE2F89; }`（深色主題 hover 底 `#3e3e3e` 由 cocoya_dark.js 既有規則處理，文字同樣變粉）。
- 驗證：`npx vite build` 通過。

## 追記：terminal 關閉鈕 (X) 色彩行為對齊（同日）
- 根因：`#btn-close-terminal` 的「×」為文字 span，預設 `#aaa`，hover 被 `.terminal-tool-btn:hover { color:#fff }` 轉白——與 header 其他品牌粉 icon（opacity 0.8）不一致。
- 修正：style.css 新增 `#btn-close-terminal span { color:#FE2F89; opacity:0.8; }`（id 選擇器優先權高於 hover 泛用規則，hover 時維持粉色，僅背景如其他按鈕變 `#3e3e42`）。
- 驗證：`npx vite build` 通過。

## 追記：AI 按鈕 hover 換圖（同日）
- 需求：toolbar AI 功能按鈕（`btn-ai-menu`）icon 為 `owl.png`，hover 時改用 `owl-hover.png`（實際檔名為 `owl -hover.png`，含空格）。
- 實作：沿用既有 `.normal-icon` / `.hover-icon` 切換機制（`style.css` 已有顯示/隱藏規則，hover-icon 僅 hover 顯示），index.html 補上第二張 img 與 class。
- **深色主題防護**：以 PIL 取樣確認兩張皆為彩色圖（owl.png 主色棕 `(136,92,82)`、owl-hover 主色粉 `#FE2F89`），不可被 `invert(1)` 反轉 → `cocoya_dark.js` 兩條反轉規則（`.normal-icon` 與 `.toolbar-btn img`）皆補 `:not([src*="owl"])` 排除。
- 備註：`style.css` 514 行 hover 時 img 轉品牌粉的 filter 對 owl-hover（本來就是 `#FE2F89`）無視覺影響。
- 驗證：`node --check` + `npx vite build` 全綠。
- **[修正]** 上例「無視覺影響」判斷錯誤——通用切換規則實際上不存在（見下條），且 filter 會重染兩張圖。

## 追記：AI 按鈕兩張 icon 並排顯示修正（同日）
- 症狀：owl.png 與 owl-hover.png 並排出現，hover 時兩張都被染成粉紅。
- 根因：normal/hover-icon 的顯示切換規則在重構後**只存在於 `#btn-run` 專屬區塊**（style.css 531 行起），沒有 `.toolbar-btn` 通用版（舊版寫在 index.html inline，已不存在）→ hover-icon 不會隱藏；且 style.css 526 行「hover 時全部 img 轉粉 filter」同時命中兩張。
- 修正：① style.css `.toolbar-btn img` 後新增通用三條切換規則（`.toolbar-btn .hover-icon{display:none}` / `:hover .normal-icon{display:none}` / `:hover .hover-icon{display:block}`）② hover 轉粉 filter 規則補 `:not([src*="owl"])` 排除（owl-hover 本身即 `#FE2F89` 彩色圖，不重染）。
- 使用者已自行將檔名 `owl -hover.png` 改為 `owl-hover.png`（index.html 已對齊）。
- 驗證：`npx vite build` 通過。

## 追記：首頁操作成功訊息對話框被首頁蓋住（同日）
- 症狀：在 Startup Home 執行「設定 Python 路徑」或「檢查 Python 套件」成功後，alert 訊息對話框被首頁（`#startup-home`, z-index **10002**）蓋在底下。
- 根因：Tauri 端自訂對話框 `.cocoya-prompt-dialog-overlay`（`tauri.js _ensureDialogStyles` 注入）z-index 為 **9999** < 首頁 10002，故永遠被蓋住。VSIX 端用 host 原生對話框不受影響。
- 修正（統一提到 **10100**，高於首頁 10002；診斷視窗本身 10050 屬同族最高，無需更動）：
  - `tauri.js` `.cocoya-prompt-dialog-overlay` z-index 9999 → 10100
  - `theme_manager.js` `.cocoya-theme-switch-overlay` z-index 10001 → 10100（同類潛在 bug：首頁也可切主題時會被蓋住）
  - `dialogs.js` 兩處動態對話框 inline zIndex 10001 → 10100（SSH 設定、訓練後端選擇，首頁上操作同風險）
- 驗證：`node --check` (tauri/dialogs/theme_manager) + `npx vite build` 全綠。

## 追記：Dataset Manager 資料夾/檔案選取預設路徑 = XML 專案根（同日）
- 需求：dataset manager 選取影像資料夾或 CSV/JSON 檔案的按鈕，原生 dialog 的起始目錄改用 XML 專案根（後端錨定 SSOT 的 projectRoot）。
- 異動：
  - `ui/src/bridge/base.js`：`pickFolder(defaultPath)` 接受起始目錄；新增 `pickDataFile(defaultPath)`（原生 dialog 選檔案 → 回 `{path, content}`）。
  - `ui/src/modules/dataset_manager/io/bridge.js`：`pickFolder(defaultPath)`、新增 `pickDataFile(defaultPath)`。
  - `ui/src/bridge/tauri.js`：`pickFolder` invoke 傳 `defaultPath`；新增 `pickDataFile` case → invoke `pick_data_file` → dispatch `dataFileSelected`。
  - `ui/src/modules/dataset_manager/application/importUseCases.js`：`importDirectory` 先 `getProjectAnchor()` 取 projectRoot 傳入 `pickFolder`；新增 `importDataFilePath()`（原生 dialog 選檔 + parseDataFileRows 匯入），`return` 補上。
  - `ui/src/modules/dataset_manager/ui_layout.js`：`importBtn.onclick` 改走 `handleDataFileImport()`（原生 dialog），保留 file input 為 fallback。
  - `src/handlers/datasetOps.ts`：`handlePickFolder` 用 `message.defaultPath || getProjectRoot()` 作 defaultUri（fallback lastDatasetFolder）；新增 `handlePickDataFile`。
  - `src/cocoyaManager.ts`：route `pickDataFile`。
  - `src/hostI18n.ts`：新增 `pickDataFileTitle` / `dataFilePickFailed`（zh/en）。
  - `src-tauri/src/commands/file.rs`：`pick_folder` 加 `default_path: Option<String>`（`set_directory`）；新增 `pick_data_file`（`blocking_pick_file` + `read_to_string` UTF-8）。
  - `src-tauri/src/lib.rs` / `permissions/commands.toml`：註冊 `pick_data_file`（二階段權限）。
  - `docs/backend_api_manifest.md`：同步 `pick_folder` 參數與新增 `pick_data_file` 列。
  - i18n：`dataset_manager/i18n/zh-hant.js`+`en.js` 新增 `DSM_STATUS_LOADING_FILE`。
- 驗證：`node --check` ×7、`tsc --noEmit`、`vite build`、`cargo check` 全綠（僅既有 warnings）。
- [ ] 實機待測：VSIX + Tauri 下資料夾/檔案 dialog 起始目錄皆為專案根。

## 追記：Dataset Manager 刪除縮圖流程的預防性保守處理（同日）
- 背景：使用者觀察 VSIX 縮圖「X」可刪縮圖/更新統計但疑似未刪真檔（Tauri 正常）；複測後確認 VSIX 可正常刪（前次可能觀察誤差）。經對照兩端：掃描的 `img.path` 語義一致（皆相對路徑正斜線）、`sourceFolderPath` 皆為 canonical、後端皆簡單 unlink/remove_file。舊架構缺陷為 **fire-and-forget**：前端 send 後無條件移除縮圖，後端「檔案不存在視為成功」→ 靜默失敗被掩蓋。
- 使用者決策：若真檔已不存在，保留縮圖/json 標註反而是錯的 → 「成功」與「檔案不存在」皆應移除縮圖；僅真實 IO 失敗才保留並提示。只做預防性保守處理，不做 confinement 大重構。
- 異動：
  - `ui_layout.js handleDeleteImage`：改 async，改用 `datasetBridge.request({command:'datasetDeleteImage', payload:{filePath}, resultCommand:'datasetDeleteImageResult', timeoutMs:10000})` 等待結果——成功 → 照舊移除；`errorCode==='FILE_NOT_FOUND'` → 移除並提示「原始檔已不存在，已從清單移除」（新 i18n `DSM_DELETE_FILE_NOT_FOUND`）；其他失敗 → 顯示錯誤並保留縮圖。`validateDeletableImagePath` 越界預檢保留。
  - `src/handlers/datasetOps.ts handleDatasetDeleteImage`：新增 `filePath`/`exists` debug log；「不存在」改回 `success:false + errorCode:'FILE_NOT_FOUND'`（不再靜默成功）。
  - `src-tauri/src/commands/file.rs delete_file`：不存在改 `Err("FILE_NOT_FOUND: ...")`，對齊 VSIX。
  - `ui/src/bridge/tauri.js datasetDeleteImage` case：錯誤解析 `CODE:` 前綴 → dispatch 帶 `errorCode`。
  - i18n：`dataset_manager/i18n/{zh-hant,en}.js` 新增 `DSM_DELETE_FILE_NOT_FOUND`。
  - `docs/backend_api_manifest.md`：`delete_file` 錯誤碼同步。
- 驗證：`node --check` ×4、`tsc --noEmit`、`vite build`、`cargo check` 全綠（僅既有 warnings）。
- [ ] 實機待測：VSIX + Tauri 刪縮圖（真刪成功提示無訊息；手動移除真檔後再刪縮圖 → 應提示「原始檔已不存在」且縮圖移除）。

## 追記：toolbar 專案 label hover 顯示完整路徑 + 視窗標題只顯示 Cocoya（同日）
- 需求：① toolbar 左方專案 label hover 時顯示完整路徑（原本只顯示檔名）② 視窗標題只顯示 Cocoya，不再帶專案檔名以免重複。
- 異動：
  - `ui/src/ui/base.js updateFileStatus`：`fileLabel.title` 改為「專案根 + 檔名」完整路徑——先以 `bridge.capabilities.projectRoot` 快照同步設定，再以 `getProjectAnchor()`（後端權威）非同步校正；未錨定/未命名 fallback 為原顯示名稱。
  - `ui/src/bridge/tauri.js setWindowTitle`：`fullTitle` 改為 `'Cocoya' + (dirty ? ' *' : '')`，不再帶檔名；保留 `*` 標記供視覺提示（Tauri 關閉攔截現行走 Rust `set_dirty` + `closeRequested`，不依賴 title 內容，已確認無破壞）。
  - `src/cocoyaManager.ts updateTitle`：`panel.title` 改為 `Cocoya` + dirty marker（移除檔名/資料夾名）；狀態列 `Cocoya Project: <path>` 暫時訊息保留。
  - **`ui/src/app/persistence.js snapshotWorkspaceForReload`**：原本從 `document.title` 剝 `*` 取檔名（title 改版後會變成 'Cocoya' 導致 reload 還原 label 錯誤）→ 改用 `window.CocoyaUI.currentFilename`。
- 驗證：全 `ui/src` 掃描 `document.title` 殘留 = 0（僅 tauri.js 寫入）；`node --check` ×3、`tsc --noEmit`、`vite build` 全綠。
- [ ] 實機待測：VSIX + Tauri label hover 完整路徑、視窗/分頁標題只顯示 Cocoya（dirty 時 `Cocoya *`）、Ctrl+R reload 快照還原檔名正確。
- **[修正]（同日）**：使用者要求 Title 檔名**改回原樣**——多檔案開啟下需靠標題辨識切換視窗。已還原：Tauri `setWindowTitle` 回 `Cocoya - <檔名>` 格式、VSIX `updateTitle` 回 `<檔名>*(folder) - Cocoya` 格式。保留：label hover 完整路徑、persistence snapshot filename 改用 `currentFilename`（比原 title 剝星號更正確）。驗證：node --check / tsc / vite build 全綠。
- **[修正]（同日）**：hover 完整路徑分隔符混用——`projectRoot` 可能為反斜線（Windows），組合檔名前先 `.replace(/\\/g,'/')` 統一正斜線（快照與權威校正兩處皆改）。驗證：node --check / vite build 全綠。
- **[修正]（同日）**：編輯模式換主題不再跳出三選對話框（保留/捨棄/取消）——`theme_manager.js setMode` 改為一律先 `snapshotWorkspaceForReload()`（快照涵蓋 dirty 與乾淨，還原即當前內容）後直接 reload；移除死碼 `_showReloadChoice` / `_ensureSwitchStyles`（含 cocoya-theme-switch-* 樣式）。驗證：node --check / vite build 全綠。





