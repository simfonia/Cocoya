// Common Generators
Blockly.Python.forBlock['common_test'] = function(block, generator) {
  // Generate Python code for the test block
  const code = 'print("Hello from Cocoya!")\n';
  return code;
};